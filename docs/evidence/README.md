# docs/evidence — スクリーンショットの置き場

**BACKLOG B24 / B33 の恒久策。** ここに置くスクショの作り方・追跡してよい条件・限界を書く。

## 何が問題だったか

一覧・詳細・統計・産地の画面には **(日付, 銘柄, 都道府県) が同じ行に写る**。これは B18 で
「コミットした2つの射影から復元できた」と問題にした**飲酒台帳の結合キーそのもの**。しかも
**PNG は画素なので `npm run ledger:check`(テキスト検査)は中身を1バイトも読めない** =
混入しても CI は緑になる(B24)。

結果、brain の品質バー「390px / 1280px のスクショを証拠にする」と「台帳を公開しない」が
正面衝突し、Phase 3〜6 の完了条件のスクショが**すべて未達のまま積んだ**(B33)。

## 決めたこと

**証拠の種類で分担する。**

| 何を | どうやって | どこに残す |
|------|-----------|-----------|
| **数値**(203本 / 年別 / 県別34区分 / 分母 185→190 / 横あふれ0) | **実データ**を実ブラウザに入れて **DOM を実測**する | `docs/phases/PHASE_*.md` の本文(コマンド出力・実測値) |
| **見た目**(レイアウト・折り返し・情報密度・下端の切れ) | **合成データ**を取り込んだ画面を撮る | `docs/evidence/demo-*.png`(追跡する) |

実データのスクショは**撮ってもよいが追跡しない**(`.gitignore` の既定が「追跡しない」)。

### 合成データで**出ない**もの(限界。埋めない)

- 203行の一覧をスクロールしたときの体裁(合成データは 27件)
- 34区分198本の県別一覧の高さ / 47県の一覧の折り返し(合成データは 23区分25本)
- 延べ314本のスタイル分布で棒が潰れる・軸ラベルが重なる現象
- 実 iPhone の写真(EXIF 回転)・実 HEIC(→ B34。実機でしか回収できない)

**これらは合成データの件数では再現しない。**「スクショがあるから見た目は確認済み」と読まない。
実データでの体裁は DOM 実測(高さ・`scrollWidth - clientWidth`・件数)で見ること。

## 追跡してよいスクショの条件

1. **記録0件の空状態**、または
2. **`docs/evidence/demo-backup.json`(合成データ)だけを取り込んだ画面**

`.gitignore` は `docs/evidence/*.png` を既定で無視し、上の条件を満たす**個別のファイル名だけ**を
`!` で戻す。この `!` の列挙が allowlist の**単一の出所**で、`scripts/check-ledger-leak.mjs` が
「index に載っている `docs/evidence/` の画像がこの列挙と一致するか」を検査する。

**新しいスクショを追跡するときの手順(飛ばさない)**

1. 撮る(下の「撮り方」)
2. **1枚ずつ画像を開いて目で見る。** 台帳の日付・銘柄・店名が写っていないことを確認する
   (画素は grep できない。**この目視が唯一の検査**)
3. `.gitignore` に `!docs/evidence/<名前>.png` を足す
4. `npm run ledger:check` が緑になることを確認する

### この仕組みで防げないこと

- **許可済みの名前のまま中身を実データのスクショに差し替える**こと。画素は読めないので
  検出できない(ファイル単位の名前 allowlist の原理的な限界)。撮り直したら 2 をやり直す。
- 目視の見落とし。合成データ以外を1枚でも混ぜないことが前提。

## 撮り方(再現手順)

```bash
npm run demo:data                         # docs/evidence/demo-backup.json を作る(決定的)
node scripts/make-demo-backup.mjs --check # 既存ファイルが生成結果と一致するか見る
npm run dev -- --port 5178 --strictPort
```

ブラウザで `http://localhost:5178/` を開き、

1. 「取り込み / 書き出し」→「取り込む JSON ファイル」で `docs/evidence/demo-backup.json` を選ぶ
2. 読んだ内容(記録 27件 / エイリアス 2件)を確認して「取り込む」
   — **デモ投入専用の裏口は作っていない**。本番と同じ2段の取り込みを通す
3. 幅 390px と 1280px で、**5タブ(記録 / 統計 / 味 / 産地 / 知る)**をビューポート撮影する

撮影は**ビューポート**(全ページではない)。下端のタブバーが切れていないこと・
折り返しが破綻していないことを、実機で見るのと同じ切り取りで見るため。

**ビューポート撮りではフッタが写らない。** `<main>` の下端にあるので、どのタブでも
折り返しの下に隠れる。フッタの体裁を証拠に残すには `<main>` を下端まで送った1枚を
別に撮る(`demo-footer-390.png`)。

### 2026-07-26: ライトテーマ移行に伴い `demo-*.png` 8枚を撮り直した

**それ以前の 8枚はダークテーマの画面で、証拠として誤りになったため上書きした。**
配色以外(文言・レイアウト・件数)は変えていないので、比較すべきは色だけ。

このときは `npm run dev` ではなく **`npm run build` の成果物を `vite preview --port 5179`**
で配信して撮った(本番と同じ CSS を通すため。手順 1〜3 自体は上と同じで、取り込みも本番と
同じ2段を通している)。撮影後に `indexedDB.deleteDatabase('sake-record')` からやり直して
合成データだけが入った状態であることを確認済み。

同じ run で実測した値(4画面 / 390px・1280px):

- 地の色は `#ffffff`、面は `#f7f7f8` / `#ececef` で **R−B は 0 / −1 / −3**
  = 中性〜わずかに寒色。クリーム/ベージュに寄っていないことを数値で確認した
- 本文・補助文字のコントラストは**全画面で 4.5:1 以上**(最小 5.08。文字要素 237/117/115/132 個を
  `getComputedStyle` で総なめして 4.5 未満は 0件)
- 地図のランプは L\* が **94.8 → 85.9 → 72.2 → 52.1 → 35.5** と単調に暗くなる
  (未進出 → 1〜2本 → 3〜5本 → 6〜10本 → 11本以上)
- 横あふれ 0(`documentElement.scrollWidth === innerWidth`)/ console の error・warning 0件

#### 同日に `demo-flavor-*` / `demo-area-*` の4枚だけ撮り直した

上の8枚のあとで、実測から見つかった**配色の欠陥3件**を直したため、その2画面だけ再撮影した
(`demo-timeline-*` / `demo-stats-*` は直した箇所が写らないので触っていない)。直したもの:

1. レーダーの目盛りが記録1本ずつの線と**同じ濃さ(対比 1.00)で区別できなかった** →
   `--color-plot-grid` を `#e4e4e7` にして記録層より薄い側へ(対白 1.27 / 対記録層 1.17)。
   散布図のセルの枠は別トークン(`--color-plot-cell-line`)に分けて元の濃さを保った
2. 散布図の凡例が「**明るい**セルは本数が多い」= 実際と逆だった(白地では多い方が濃い)→「濃い」
3. 県別一覧の棒が対白 1.12〜1.65 でほぼ見えていなかった → 不透明度 30% → **60%**
   (棒 1.25 / 1.59 / 2.26 / 3.01、棒の上の文字は最悪 5.32 で 4.5 を割らない)

再撮影分で実ブラウザから読んだ値(canvas に塗って読む。`getComputedStyle` は不透明度付きの
色を `oklab()` で返すので数値を素朴に取ると誤読する):
目盛り `#e4e4e7`(対白 1.27)/ 記録層 `#d4d4d4`(1.48)/ 目盛り 対 記録層 1.17 /
外周 `#a1a1aa`(2.56)/ 平均 `#27272a`(記録層に対して 10.05)/ 散布図のセルの枠 `#d4d4d8`(1.48)/
一覧の棒 `#fde594`(1.25・1〜2本)`#f9c56d`(1.59・3〜5本)。
「すべて消す」ボタンの枠は `#f43f5e` で**自分の面の上で 3.34**(直す前は 2.18)。
横あふれ 0 / console の error・warning 0件も同じ run で確認した。

### 2026-07-27(5): タブを読む人の関心で割り直し、産地に土地の話を入れた

**利用者の指摘**（「数え方」というタブが何の話か分からない / 産地は特徴を書くべき /
「日本酒」タブという名前は区別にならない）を受けて、**タブの切り方そのものを変えた**:

    旧: 数え方 / 味 / 産地 / 日本酒 / 季節 / 出典
    新: 種類 / ラベル / 季節 / 産地 / 味 / アプリ

日本酒そのものの話を前に出し、実装の都合（数え方・紐付け・保存・出典）は「アプリ」1枚に寄せた。
産地には**酒どころ・蔵の数（同梱データの県別集計）・酒米**を足し、地図の見方は1節に圧縮した。

同じ run で実測した値(390px):

- パネルの高さ **種類 1,292 / ラベル 1,213 / 季節 500 / 産地 1,278 / 味 1,333 / アプリ 2,006px**
  （組み替え前は 日本酒だけ 2,465px と突出していた）
- 横あふれ 0 / console の error・warning **0件**
- 蔵の数は同梱 `breweries.json` を `areas.json` の索引で県名に直して数えた値
  （**1,749蔵 / 47都道府県すべてに蔵がある**。新潟113 / 兵庫97 / 長野93 / 山形76 / 福島70 / 福岡57）

### 2026-07-27(4): 厳密さの要求を下げ、「日本酒」タブに中身を入れた

**利用者の判断で方針が変わった**（私用のアプリなので法令上の厳密さは求めない）。出所の3値バッジ・
「確認できていない」の断り・慣習の印を**すべて外し**、代わりに**平易な説明**を入れた。
タブ「名称」は**「日本酒」**になり、日本酒とは / 特定名称8種の表 / ラベルでよく見る語 /
ラベルの数字 の4節になった。撮り直し4枚 + 新設2枚(`demo-learn-sake-390` / `demo-learn-terms-390`)。

- パネルの高さ(390px): 数え方 1,161 / 味 1,333 / 産地 912 / **日本酒 2,465** / 季節 500 / 出典 837px
- 横あふれ 0 / console の error・warning 0件
- **8種の表の値は告示から写したまま**(出典と取得日は出典タブに移した)

### 2026-07-27(3): 「知る」に保存と季節を足し、下位タブを6つにした

**「知る」の拡充**（記録の保存とバックアップ / 8種に当たらない清酒 / 季節の呼び名）に伴い、
`demo-learn-390` / `-1280` / `demo-footer-390` を撮り直し、**`demo-learn-season-390.png` を新設**
した(追跡 17枚)。**法令の原文（酒税法・GI「日本酒」・告示の第4〜6項）はこの環境から取得できない**
(`www.nta.go.jp` / `laws.e-gov.go.jp` へのアクセスがネットワークポリシーで 403)ため、
**法令由来の項目は入れていない**。入れたのは (1) 実装から引ける値 (2) 慣習の呼び名（印付き）だけ。

同じ run で DOM から実測した値(390px / 1280px):

- **タブは6つ**(数え方 / 味 / 産地 / 名称 / 季節 / 出典)。パネルの高さは 390px で
  **1,161 / 1,333 / 912 / 2,586 / 557 / 837px**、1280px で 785 / 999 / 639 / 2,044 / 447 / 585px
- 季節を「名称」から独立させた結果、名称は 3,123 → **2,586px**(季節を足す前が 2,264px)
- タブ帯は6列でも1段に収まる。最長タブを下端まで送っても `tablist` の `top` は 45px のまま
- 横あふれ 0 / console の error・warning **0件**

### 2026-07-27(2): 「知る」を5つの下位タブに割り、5枚を撮り直し・2枚を足した

**目次を足しただけでは「長い1枚」のままだったので、下位タブ（数え方 / 味 / 産地 / 名称 / 出典）に
割った。** 同時に味と産地を図（棒・チップ・スウォッチ）にし、産地の説明を新設したので、
`demo-learn-390` / `-1280` / `demo-footer-390` を撮り直し、
**`demo-learn-flavor-390.png` と `demo-learn-area-390.png` を新設**した(追跡 16枚)。

同じ run で DOM から実測した値(本番ビルドを `vite preview --port 5179`):

- **1枚 5,274px → タブごとに 856 / 1,333 / 912 / 2,264 / 837px**(390px 幅。`role="tabpanel"` の
  高さ)。1280px では 557 / 999 / 639 / 1,840 / 585px。**最長でも「知る」全体の半分以下**
- **タブ帯は上端に貼り付く。** 最長の「名称」を下端まで送っても `tablist` の `top` は 45px
  (= `<main>` の上端)のままで、どこまで読んでも別のタブへ移れる
- **切り替えるとスクロールは先頭へ戻る**(`scrollTop` 0 を各タブで確認)
- 横あふれ 0(`documentElement.scrollWidth === innerWidth`)/ console の error・warning **0件**
  (5タブ × 390/1280 の全 run)
- **開いていないタブの中身は DOM に無い**(単体テストが固定)。義務のある表示の所在は変わらない
  — さけのわの1行は全画面のフッタ、CC-BY の4項目は**使用箇所である産地タブ**に併記のまま

### 2026-07-27: 「知る」の構造化に伴い3枚を撮り直した

**「知る」に目次・節番号・区切り・「目次へ戻る」を入れたので、その面が写る3枚が古くなった。**
撮り直したのは `demo-learn-390.png` / `demo-learn-1280.png` / `demo-footer-390.png` の3枚
(他の8枚には「知る」の本文が写らないので触っていない)。追跡している枚数は 14枚のまま
= `.gitignore` の allowlist に変更なし。

配信は `npm run build` の成果物を `vite preview --port 5179`、取り込みも本番と同じ2段。
**内容・文言は1文も変えていない**ので、比較すべきは構造(目次の有無・見出しの段差)だけ。

同じ run で DOM から実測した値:

- **目次から節へ着く。** 「4 出典とライセンス」を押すと `main.scrollTop` が 0 → **4,409px**、
  「2 特定名称の8種類」で **2,167px**。どちらも見出しが画面上端(`top` 45px = ヘッダの直下)に
  着き、`document.activeElement` がその見出しになる(スクロールとフォーカスが一致する)
- 「目次へ戻る」を押すと目次が上端に戻り、`activeElement` が目次になる
- **見出しの段差**: `h2` 16px / `h3` 14px、色はどちらも `rgb(24,24,27)`(`text-ink`)。
  撮り直す前は `h2` 14px / `h3` 12px `rgb(82,82,91)` で、**小見出しが本文中の太字より弱かった**
- **「知る」の高さ**: 390px で 4,480 → **5,274px**(目次 347px ぶん増えた) / 1280px で 3,169 → 3,888px。
  **短くはなっていない**(畳まない判断をしたため。B63)
- 表2つは親の `overflow-x-auto` の中だけで横スクロールする(390px で 544/358・480/358)。
  横あふれ 0(`documentElement.scrollWidth === innerWidth`) / console の error・warning **0件**

### 2026-07-26: 5タブ化とフッタ1行化に伴い `demo-*.png` を撮り直し、2種類を足した

**それ以前の 8枚は「4タブ + フッタ3段落」の画面で、器が変わったので証拠として誤りになった。**
10枚(5タブ × 390/1280)を上書きし、`demo-footer-390.png` を新設して合計11枚にした。

配信は `npm run build` の成果物を `vite preview --port 4173`(本番と同じ CSS とバンドルを
通すため)。取り込みも本番と同じ2段(ファイルを選ぶ → 内容を確認して「取り込む」)を通した。

同じ run で DOM から実測した値:

- **5タブが1段に収まる。** `nav` の `grid-template-columns` は 390px で `78px × 5`、
  5つのボタンの `getBoundingClientRect().top` が全部同じ(= `.grid-cols-5` が本番 CSS に
  生成されている。リテラルで書く約束が効いている証拠)
- **フッタは1行。** `clientHeight` 44px / `getClientRects().length` 1 / `<p>` 0個 /
  中身は「さけのわデータを利用しています」(→ `https://sakenowa.com`)と「出典とライセンス」の2つだけ。
  **5タブすべてで同じ**。20歳未満の表記と CC-BY の4項目はフッタから消えている
- **CC-BY の4項目は産地タブと「知る」の出典節にある**(フッタから外した移設先2箇所)
- 横あふれ 0(`documentElement.scrollWidth === innerWidth`)を5タブ × 390/1280 で確認
- 「知る」の表2つは親の `overflow-x-auto` の中だけで横スクロールし(544px / 480px を
  358px の枠で送る)、`body` は溢れない
- console の error・warning **0件**(タブ移動・検索・絞り込み・取り込み3回を含む全 run で0)

### 合成データの中身と、それが満たす不変条件

`scripts/make-demo-backup.mjs` が生成する 27件。生成時と `ledger:check` の**両方**が検査する:

1. `drankOn` が**台帳の日付集合と交わらない**(台帳は 2020..2026 / 合成は 2017..2019)
2. 銘柄表記・銘柄名が**台帳の銘柄集合(94種)と1つも一致しない**

都道府県だけは実在の県名を使う(産地の塗り分けの証拠に要る)。県名は
`linkBrand.cases.json` で公開済みの列で、**日付と同居しなければ結合キーにならない** —
1 によって同居し得ない。`place` は全行に「デモ」を含めてあるので、
**スクショ1枚だけを見ても合成データだと分かる**。

銘柄はさけのわの公開マスタから採った実在の銘柄(架空名だと `sakenowaBrandId` が引けず、
味タブのレーダー・散布図が空になって証拠にならない)。**実在するが本人が飲んでいない銘柄 ×
架空の日付・場所・備考**という組み合わせ。

`--check` を `npm run invariants` に入れていないのは、生成物が上流(さけのわ)の銘柄IDに
依存するため。上流が銘柄を消した日に**月次更新ジョブがデモ用 fixture のせいで赤になる**のは
割に合わない。CI が守るのは**生成の由来ではなく中身**(上の不変条件2つ)。

## 置いてあるもの

| ファイル | 内容 | データ |
|---|---|---|
| `demo-backup.json` | 撮影に使う合成データ(記録27 / エイリアス2 / サムネイル2) | 合成 |
| `demo-timeline-390.png` / `-1280.png` | 記録タブ(5値のバッジ・写真付きの行・長文の折り返し) | 合成 |
| `demo-stats-390.png` / `-1280.png` | 統計タブ(総本数・年別・都道府県別) | 合成 |
| `demo-flavor-390.png` / `-1280.png` | 味タブ(分母の明示・6軸レーダー) | 合成 |
| `demo-area-390.png` / `-1280.png` | 産地タブ(塗り分け・凡例・塗れなかった件数・47県一覧・CC-BY の4項目) | 合成 |
| `demo-learn-390.png` / `-1280.png` | 知るタブ(**下位タブ帯**・既定の「種類」) | 合成 |
| `demo-learn-flavor-390.png` | 知るの「味」タブ(上位語の棒・種類ごとの語のチップ) | 合成 |
| `demo-learn-area-390.png` | 知るの「産地」タブ前半(酒どころ・県別の蔵の数) | 合成 |
| `demo-learn-terms-390.png` | 知るの「ラベル」タブ(11語 + よく見る語の説明) | 合成 |
| `demo-learn-season-390.png` | 知るの「季節」タブ(新酒・ひやおろし等を時期つきで) | 合成 |
| `demo-learn-region-390.png` | 知るの「産地」タブ後半(酒米・地図の見方) | 合成 |
| `demo-footer-390.png` | フッタ1行 + 知るの「アプリ」タブ末尾(CC-BY の4項目・OCR・告示の取得日) | 合成 |
| `demo-browse-area-390.png` | 記録フォームの「一覧から選ぶ」1段目(47県 + その他 と蔵元の数) | 記録0件 |
| `demo-browse-brewery-390.png` | 同 2段目(福島県の蔵元52件と銘柄の数) | 記録0件 |
| `phase1-1280.png` / `phase1-390-offline.png` | Phase 1 の空状態 / オフライン | 記録0件 |
| `phase3-390-empty.png` | Phase 3 の空状態 | 記録0件 |

`phase3-390.png` / `phase3-1280.png`(実データ)は**追跡しない**。手元に残っていても
`.gitignore` の既定で無視されるので `git status` にも出ない。
