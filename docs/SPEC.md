# SPEC — 日本酒 記録アプリ（saketime）

## 目的 / 背景

2020-12 から続けている日本酒の飲酒記録（203本 / Excel + 写真）を、記録しやすく振り返りやすいアプリに置き換える。

現状の記録は「何をいつ飲んだか」の台帳でしかなく、**味の傾向を振り返る手段がない**。自分で味の軸を毎回入力するのは飲みながらでは続かないため、**銘柄に紐づく味のデータは外部（さけのわデータ）から取得し、自分は評価とメモだけ入れる**という分担にする。

できあがりの状態: スマホで写真を選んで銘柄をサジェストから選ぶだけで1本が記録でき、PC でも同じ URL を開いて「時系列 / 統計 / フレーバー分布 / 産地マップ」の4視点で203本+今後の記録を振り返れる。

### 外部データ: さけのわデータ（調査済み・実測）

| 項目 | 実測値 |
|---|---|
| 提供元 | さけのわデータプロジェクト https://muro.sakenowa.com/sakenowa-data/ |
| 費用 / 商用 | 無料。商用・非商用可、加工可 |
| **義務** | **クレジット表示 + `https://sakenowa.com` へのリンクが必須**（省略は禁止事項） |
| エンドポイント | `/api/` 配下に `areas` `brands` `breweries` `rankings` `flavor-charts` `flavor-tags` `brand-flavor-tags` |
| 銘柄数 | 3264件。うちフレーバーチャート付き 1344件 |
| フレーバー6軸 | `f1`華やか / `f2`芳醇 / `f3`重厚 / `f4`穏やか / `f5`ドライ / `f6`軽快、各 0.0〜1.0 |
| 生データ量 | 6endpoint 計 約608KB（brands 156KB / flavor-charts 218KB / brand-flavor-tags 147KB / breweries 81KB 他） |
| **CORS** | **`Access-Control-Allow-Origin` ヘッダなし。ブラウザから直接 fetch できない** |

**CORS が無いことが構成上の決定要因**: 静的ホスティング + バックエンド無しでは実行時取得が不可能なため、**ビルド前に Node スクリプトで取得してリポジトリにコミットし、バンドルする**。結果としてオフライン動作とサジェストの即応性も得られる。

### 既存203本との突合率（実測済み・実装前の実現可能性検証）

`brain/40_Resources/sake-log.md` の全203本をさけのわ銘柄マスタに突合した結果:

| 分類 | 種数 | 本数 | 割合 |
|---|--:|--:|--:|
| 名称+都道府県が完全一致 | 76 | 173 | 85% |
| エイリアス表で回収可能 | 8 | 13 | 6% |
| **紐付け可能 合計** | | **186** | **92%** |
| 紐付け不能（手動対応） | | 17 | 8% |

一致した173本のうち**172本にフレーバーチャートが存在する**（欠けるのは「ビキニ娘」1本のみ）。よく飲む銘柄はチャートが揃っており、外部データを軸にする方針は成立する。

**必要なエイリアス表（実測で確定した8件）**

| ログ表記 | さけのわ銘柄 | brandId | 蔵元 | 本数 |
|---|---|--:|---|--:|
| 赤武 | AKABU | 2602 | 赤武酒造(岩手) | 4 |
| 髙砂 | 高砂 | 9941 | 木屋正酒造(三重) | 2 |
| 寒菊 / 寒菊(OCEAN99) | 総乃寒菊 | 1349 | 寒菊銘醸(千葉) | 2 |
| ZEBRA / MAGMA | 栄光冨士 | 1217 | 冨士酒造(山形) | 2 |
| 荷札酒 | 加茂錦 | 3534 | 加茂錦酒造(新潟) | 1 |
| 会津宮泉 | 宮泉 | 2401 | 宮泉銘醸(福島) | 1 |
| ゆきのまゆ | ゆきのまゆ（醸す森） | 41721 | 苗場酒造(新潟) | 1 |

「高砂」はさけのわに5件（静岡/三重/佐賀/島根）あり、**都道府県で一意に絞れる**。エイリアスは銘柄名だけでなく都道府県との組で解決する。

**紐付け不能な17本（手動紐付けUIの対象）**

- `不明` 5本 — ログ時点で銘柄が判読できていない
- **`寫楽` 5本 — さけのわに「寫楽」「写楽」いずれの登録も無い。**蔵元の宮泉銘醸は `宮泉`(2401) として存在するため、代替紐付けするかは本人判断に委ねる
- `清開` `無量山` `龍吟虎嘯` `寿限無` `雷` 各1本 — さけのわ未登録
- `英君 または 英勲` 1本 — 両方実在（英君=静岡1132 / 英勲=京都728）し、写真から確定できていない
- `Beau Michelle` 1本 — 同名がさけのわにあるが所在地が異なる（ログ:神奈川/川西屋酒造 ↔ さけのわ:長野）。別物の可能性が高く自動紐付けしない

## スコープ

### やること

1. **記録の作成・編集・削除** — 写真（端末の写真アプリで撮影済みのものを選択）、日付、銘柄（サジェスト）、スペック（自由文）、5段階評価、飲んだ場所・店名、メモ
2. **銘柄サジェスト** — バンドル済み3264件をインクリメンタル検索。選択で都道府県・蔵元・フレーバー6軸が自動で埋まる
3. **手動紐付けUI** — 未紐付けの記録に対し、さけのわ銘柄を検索して手動で紐付ける。紐付け結果はエイリアスとして永続化し、以降の同名表記に自動適用
4. **4つの閲覧画面**
   - 時系列リスト（サムネイル付き・新しい順・検索/絞り込み）
   - 統計ダッシュボード（本数・年別・都道府県別・スペック分布）
   - フレーバー分布図（6軸レーダー / 散布図。自分がなぞっている味の領域と空白地帯）
   - 産地マップ（都道府県SVGを本数で塗り分け、未進出県を可視化）
5. **インポート / エクスポート** — 全記録をJSONで入出力。既存203本の取り込みも同じ経路
6. **さけのわデータの定期自動更新** — GitHub Actions で月次取得し、差分があればコミット→再デプロイ
7. **PWA** — ホーム画面追加・オフライン動作
8. **クレジット表示** — さけのわデータのクレジットと `https://sakenowa.com` へのリンクを常設

### やらないこと（恒久）

- **SNS共有・公開機能** — 記録を外部に共有・投稿する導線は作らない。データは端末内に閉じる

### やらないこと（今回のリリース。将来の拡張余地として残す）

- ラベル写真からのOCR / AI自動認識（銘柄はサジェスト検索で選ぶ）
- 在庫管理・未開栓管理（「飲んだ記録」に徹する）
- 価格・購入先・同席者・温度帯の記録項目
- 認証・クラウド同期（下記「同期しない」は保存方式の決定に由来する制約）

### 決定に由来する制約（トレードオフとして受け入れる）

- **端末間同期は無い。** 保存先を IndexedDB のみとしたため、別端末へはJSONエクスポート/インポートで移す
- **ブラウザのサイトデータ削除で記録が消える。** 定期的なJSONエクスポートが唯一のバックアップ手段。アプリ側で「最終エクスポートからの経過日数」を警告表示して緩和する
- **写真の原本はアプリが持たない。** カメラアプリで撮った原本は端末のカメラロールに残り、アプリは長辺400pxのサムネイルのみ保持する

## 前提（本SPECで採用する技術判断）

| 項目 | 決定 | 理由 |
|---|---|---|
| スタック | Vite + React + TypeScript | poker-gto と同構成。既存の CI/デプロイ知見をそのまま流用できる |
| ドメイン層 | `src/domain/` は React 非依存の純TS | 既存方針「計算コアをフレームワーク非依存に保つ」。外部依存なしで単体テストする |
| 永続化 | IndexedDB（薄いラッパを自作、`src/store/`） | 依存を増やさない。スキーマが単純（1ストア+索引） |
| テスト | Vitest | 単一テスト実行が速い |
| 評価 | 5段階整数 + 未評価(null) | 飲みながらの入力を最短にする。既存203本は未評価として取り込む |
| 配信 | GitHub Pages (public) + `noindex` meta | 無料。データは端末内なので記録自体は公開されない |
| 命名 | **リポジトリ名・base パス・PWA識別子にブランド名を埋め込まない。** base はビルド設定の1箇所（`vite.config.ts` の `base`）から供給し、参照はすべて相対化する | `saketime.jp` は SAKETIME株式会社（日本酒レビューサイト）が運営中で名称が衝突する。改名が表示層の文字列だけで済む構成にしておく |

## 対象ファイル / インターフェース

```
scripts/
  fetch-sakenowa.mjs        さけのわ6endpointを取得→スリム化→src/data/sakenowa/*.json を生成
  import-sake-log.mjs       brain/40_Resources/sake-log.md → seed JSON（203本）に変換
  check-attribution.mjs     ビルド成果物に sakenowa.com リンクが含まれることをCIで検証
src/
  data/
    sakenowa/               生成物（コミットする）: areas / breweries / brands / flavorCharts / flavorTags / brandFlavorTags
    brand-aliases.ts        手書きエイリアス表（上表8件が初期値）
  domain/                   React非依存・純TS
    types.ts                SakeRecord / SakenowaBrand / FlavorChart / LinkStatus
    normalize.ts            銘柄名の正規化（NFKC・旧字体・括弧内除去・空白除去）
    linkBrand.ts            正規化+エイリアス+都道府県で brandId を解決
    stats.ts                本数・年別・都道府県別・スペック分布の集計
    flavor.ts               記録集合→6軸の平均/分布/空白地帯
  store/
    db.ts                   IndexedDB ラッパ（open / put / getAll / delete / clear）
    records.ts              SakeRecord の CRUD と索引
    backup.ts               JSON エクスポート / インポート（スキーマ版番号付き）
  ui/
    RecordForm/             記録の作成・編集（写真選択・銘柄サジェスト・評価）
    Timeline/               時系列リスト
    Dashboard/              統計
    FlavorMap/              フレーバー分布図
    AreaMap/                産地マップ（都道府県SVG）
    LinkBrand/              手動紐付け
    Attribution/            さけのわクレジット（全画面共通フッター）
public/
  sw.js                     Service Worker（シェルは原子的にプリキャッシュ、画像は best-effort）
.github/workflows/
  update-sakenowa.yml       月次でさけのわデータを取得し差分があればコミット
```

### 主要な型

```ts
type LinkStatus = 'auto' | 'alias' | 'manual' | 'unlinked' | 'unknown'

type SakeRecord = {
  id: string                    // uuid v4
  drankOn: string               // 'YYYY-MM-DD'
  brandLabel: string            // 本人が記録した生の表記（ログの「寫楽」等をそのまま保持）
  sakenowaBrandId: number | null
  linkStatus: LinkStatus
  prefecture: string | null     // 紐付け時はさけのわ由来、未紐付け時はログ/手入力由来
  spec: string                  // 「純米大吟醸 無濾過生原酒」等の自由文
  rating: 1 | 2 | 3 | 4 | 5 | null
  place: string                 // 飲んだ場所・店名
  note: string
  thumbnail: Blob | null        // 長辺400px・JPEG
  createdAt: string             // ISO8601
  updatedAt: string
}

type FlavorChart = { brandId: number; f1: number; f2: number; f3: number; f4: number; f5: number; f6: number }

// 紐付け解決。都道府県は同名銘柄の曖昧性解消に使う（「高砂」が5件あるため必須）
function linkBrand(label: string, prefecture: string | null): {
  brandId: number | null
  status: LinkStatus
  candidates: SakenowaBrand[]   // 一意に決まらないとき、手動紐付けUIに渡す候補
}
```

### 不確実性の表示規約

既存方針「不確実性は隠さず信頼度を明示する」に従い、**紐付いていない記録にフレーバー値を推定で埋めない**。

- `unlinked` / `unknown` の記録は、フレーバー分布図で「フレーバー未取得」として件数のみ表示し、6軸の集計から除外する
- 集計は必ず「分母 = フレーバー取得済み件数」を併記する（203本中186本、のように）
- `linkStatus` を UI のバッジとして出す。`manual` は本人が判断した紐付けであることが分かるようにする

## 受け入れ基準

- [ ] `npm run fetch:sakenowa` がさけのわ6endpointを取得し、`src/data/sakenowa/` に生成する。生成後の合計サイズが gzip 後 200KB 以下
- [ ] `npm run import:sake-log` が `sake-log.md` の203行を1本も落とさずJSONに変換する（件数が203であることをスクリプト自身が検証し、不一致なら非ゼロ終了）
- [ ] 203本に `linkBrand()` を適用したとき、**186本以上（92%以上）が `auto` または `alias` で紐付く**
- [ ] **回帰**: 正規化ロジックの変更で、名称+都道府県が素で一致していた173本のうち1本も紐付けが壊れない（この173本のbrandIdを固定したテストを置く）
- [ ] 紐付かない17本が `unlinked`（不明5本は `unknown`）として区別され、フレーバー集計の分母から除外される
- [ ] 手動紐付けUIで「寫楽」を任意の銘柄に紐付けると、他4本の「寫楽」にも自動適用される（エイリアスとして永続化される）
- [ ] 銘柄サジェストが3264件に対しインクリメンタル検索でき、オフラインでも動作する
- [ ] 写真を選択すると長辺400pxにリサイズされて保存され、元ファイルのサイズに関わらずレコード1件あたりのサムネイルが50KB以下
- [ ] 4画面（時系列 / 統計 / フレーバー分布 / 産地マップ）がすべて203本の実データで表示される
- [ ] 統計ダッシュボードの本数・年別・都道府県別が `sake-log.md` のサマリ節の数値と一致する（203本 / 2022:65 / 福島県22 等）
- [ ] JSONエクスポート→ブラウザのサイトデータ削除→インポートで、全記録がサムネイル込みで復元される
- [ ] 全画面にさけのわのクレジットと `https://sakenowa.com` へのリンクが表示される
- [ ] `check-attribution.mjs` がビルド成果物にクレジットリンクが無い場合CIを落とす
- [ ] `noindex` meta がビルド成果物に含まれる
- [ ] iOS Safari で下端のUIが画面外に出ない（`100vh` ではなく `100dvh` を使う）
- [ ] PWA としてホーム画面に追加でき、機内モードで起動して記録の閲覧と新規作成ができる
- [ ] ブランド名（`saketime`）がビルド設定の `base` と表示文字列以外に出現しない

## end-to-end 検証手順

```bash
# 1. 外部データ取得（CORS制約によりビルド時取得。実行時fetchではない）
npm run fetch:sakenowa
#    → src/data/sakenowa/*.json が生成される。件数を確認:
#      brands 3264件 / flavorCharts 1344件 が取得できていること

# 2. 既存203本の取り込みデータを生成
npm run import:sake-log
#    → 203件であることをスクリプトが検証して出力する

# 3. 紐付け率の検証（実現可能性の要。ここが92%を切ったら設計を見直す）
npm test -- linkBrand
#    → 「203本中186本以上が紐付く」「素で一致していた173本が壊れない」の2テストが通ること

# 4. 単体テスト（ドメイン層のみ。外部依存なし）
npm test -- domain

# 5. 型チェック + ビルド
npm run check && npm run build

# 6. 成果物の不変条件（人間の注意力ではなくgrepで守る）
node scripts/check-attribution.mjs dist/
#    → sakenowa.com へのリンクと noindex meta の存在を検証

# 7. 実機確認（PCブラウザ）
npm run preview
```

7以降は画面で確認する。

8. インポート画面から手順2のseed JSONを読み込む → 時系列リストに203本が並ぶ
9. 統計ダッシュボードを開く → 総本数203、2022年65本、福島県22本が `sake-log.md` のサマリと一致する
10. フレーバー分布図を開く → 6軸の分布が描画され、**「203本中186本のデータで集計（17本はフレーバー未取得）」のように分母が明示されている**
11. 産地マップを開く → 福島・和歌山・山形が濃く、未進出県が空白で分かる
12. 「寫楽」の記録を開く → `unlinked` バッジが出てフレーバー欄が空。手動紐付けUIで `宮泉` に紐付ける → 他4本の「寫楽」にも自動適用され、フレーバー分布の分母が191本に増える
13. 新規記録: 端末の写真を選択 → 銘柄サジェストで「紀土」を選ぶ → 和歌山県・蔵元・フレーバー6軸が自動で埋まる → 評価4、場所「自宅」で保存 → 時系列リストの先頭に出る
14. JSONエクスポート → ブラウザのサイトデータを削除 → 再読込（0件になる）→ エクスポートしたJSONをインポート → 204本がサムネイル込みで復元される
15. スマホ実機: 公開URLをホーム画面に追加 → 機内モードで起動 → 記録の閲覧と新規作成ができる。画面下端のUIが切れていない

**完成の判定**: 手順1〜6がすべて成功で終了し、手順8〜15が期待通りに観測できること。とくに手順10と12（紐付かないものを推定で埋めず、分母を明示し、本人の判断で紐付けられる）が本アプリの中核であり、ここが崩れているなら未完成とする。

## 参照

- さけのわデータ https://muro.sakenowa.com/sakenowa-data/ — 利用規約とエンドポイント仕様
- `brain/40_Resources/sake-log.md` — 既存203本の記録（インポート元）
- `brain/30_Tech_Notes/architecture-decisions.md` — 依存方向の固定 / 不確実性の明示 / ブランド名を識別子に埋め込まない
- `brain/30_Tech_Notes/deployment-and-ci.md` — サブパス配信 / SW プリキャッシュの分離 / CI grep による不変条件 / `100dvh`
