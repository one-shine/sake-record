# PLAN — <機能名>

実装計画。`docs/SPEC.md` を入力に作る。**小規模はこの1枚で完結**。大規模はフェーズに分割し、各フェーズを `docs/phases/PHASE_n.md` に切り出してここは index にする。
<!-- Explore → Plan → Code → Commit。スコープが明確で1文で差分を言えるなら、この計画もフェーズ分割も省略してよい。 -->

## Explore（所見）
<!-- 関連コードを読んで分かった現状・既存パターン・再利用できるもの。 -->

## 設計方針
<!-- 採るアプローチと理由。代替案は要点だけ。 -->

## 規模判定
- **小規模** → 下の「単一の段取り」で進める。フェーズ分割なし。
- **大規模**(複数ファイル/複数のまとまり) → 「フェーズ一覧」に分割する。

## 単一の段取り（小規模）
<!-- 完了判定できる単位で。各ステップに検証を持たせる。 -->
1. <ステップ> — 検証:
2. <ステップ> — 検証:

## フェーズ一覧（大規模）
| Phase | 目的 | 状態 | 詳細 |
|---|---|---|---|
| 1 | <目的> | 未着手 | `docs/phases/PHASE_1.md` |
| 2 | <目的> | 未着手 | `docs/phases/PHASE_2.md` |

各フェーズは「完了判定できる単位」。詳細・完了条件・フェーズ末レビュー結果は各 PHASE ファイルに書く。

## 検証 / Commit
- 各段 / フェーズ末で test・build を回し、**証拠**(出力・スクショ)を示す。別文脈の `code-reviewer` でレビュー。
- 進捗・課題の正典は `docs/BACKLOG.md`。コミットメッセージに課題ID を付け、課題↔変更を追えるようにする。
