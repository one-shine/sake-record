# BACKLOG — 日本酒 記録アプリ

**進捗・課題の単一の真実源(single source of truth)。セッション開始時にまずここを読む。**
フェーズ末レビューや実装中に出た「気になる点・スコープ外の発見・不具合」をここに起票する。

## 進捗
- 現在: **Phase 1〜5 = done / Phase 6・7 は実装完了だが未達があり done にしていない**（PLAN のフェーズ表も同様）。**受け入れ基準 A1〜A17 は 14達成 / 3未達（A7 A15 A16 = すべて iPhone 実機が必要）**。**次の1手は (a) iPhone 実機での e2e手順15 (b) `update-sakenowa.yml` の `workflow_dispatch` 実走(B9) (c) `/release`**。コミット・push・`gh workflow run` は未実施（オーケストレーターが行う）
- Phase 7 の結果: `npm run ci` **exit 0**(**40 files / 801 passed | 3 skipped**。Phase 6 は 36/729) / `npm audit --audit-level=high` **0 vulnerabilities** / `check` `build` `ledger:check`(178ファイル) `attribution:check` すべて緑 / 本番ビルドを実 Chromium で駆動して **e2e手順1〜14 を通した**(取り込み203 = 紐付186・未紐付12・不明5・フレーバー185 → 手動紐付けで分母 **185→190(93%)** → 写真1280×800 → **400×250 / 8,073B** で新規1本 → **204件** → 書き出し **86,940B** → `deleteDatabase` → 取り込みで **204件+別名1**・サムネイル Blob 8,073B 一致) / console error・warning **0件** / SW=activated・caches 13件・4タブすべてにクレジット4リンク。証拠は `docs/phases/PHASE_7.md`
- **Phase 7 の未達5件**（done にしない理由）: (1) **e2e手順15 / A7 / A16 = iPhone 実機が必要**（Chromium での代替は「配信元を止めて204件描画・`brands.json` 3264件がキャッシュから読める・新規1件で205件」= **機構まで**） (2) **A15（下端が切れない）は Chromium で `dvh == vh` のため原理的に検出不能 = 実機スクショが唯一の証拠** (3) **B9 の `workflow_dispatch` 実走が未実施**（対外的な操作なので実行しない。定義は `fetch`→`data:check`→`test`→差分判定→commit→deploy の順で入り、deploy に `ref = 新 sha` を渡す） (4) **B7 の案内側**（`persist()` 要求は2経路に入ったが `BackupNag` は書き出し画面の中だけ = フォームから始めた人は案内を見ない） (5) `public/screenshots/*` と `README.md` が未着手 → **B42**
- 罠(Phase 7 で踏んだ): **SW の「明示的な失敗応答」が反転していた**（`new Response('', {statusText:'オフラインでキャッシュにも無い'})` は `statusText` が **ByteString(ISO-8859-1)** なので日本語で **Response の構築自体が TypeError** → `respondWith` が reject し、避けようとしていた素のネットワークエラーそのものになっていた。実測: preview 停止下で 504 ではなく `Failed to fetch` / `net::ERR_FAILED`。副作用で `tables.ts` の原因を名指しする文言が到達不能）→ 日本語は本文へ・`statusText` は `'Gateway Timeout'`。**根の原因はこの関数を覆うテストが0本だったこと** → `src/pwa/sw.test.ts` 13件を新設（出荷される `public/sw.js` を `?raw` で読む。写しを作らない）= **B38**
- Phase 7 で起票: **B37**(銘柄不明の記録で削除確認が「」と空になる) **B39**(`sw.js` の `cache.put` が 206 を通す・未観測) **B40**(月次ジョブの cron が JST 毎月2日・コミット前に `tsc`/`lint`/`build` を回していない) **B41**(件数固定テストにより上流が1件増えるだけで月次ジョブが赤で止まる) **B42**(manifest の `screenshots` と `README.md` が未着手) / 回収: **B21 B23 B24 B33 B38**(done) **B34**(1)(4)(5) / **未対応で持ち越し: B1 B2 B5 B6 B7 B9 B10 B11 B16 B25 B26 B30 B31 B32 B34 B35 B36 B37 B39 B40 B41 B42**
- Phase 6 の結果: `npm run ci` **exit 0**(**36 files / 729 passed | 3 skipped**。Phase 4/5 は 29/602) / 本番ビルドを実 Chrome で駆動し **A10**(203 / 年別 1,12,65,33,31,33,28 / 福島22・和歌山20・山形17 / 34区分198本 / スタイル11語一致・延べ314・「重複計上」明記) **A5表示**(**203本中185本(91%)** ＋ 未紐付12・不明5・チャート無し1 / レーダー+散布図・空白161of240) **産地**(path 47・一覧47県・未進出14県・「塗れなかった6本」) **A9**(4画面すべて203本・console error/warn 0・390/1280 で横あふれ0) を観測。手動紐付けで分母 **185→190**(reload 後も維持)・解除で185 → **B29 と B1(3) を回収**。証拠は `docs/phases/PHASE_6.md` の「検証の証拠」
- **Phase 6 の未達2件**（done にしない理由）: (1) **CC-BY-4.0 クレジットのフッター実地確認**（`attribution:check` は成果物に4項目が在ることまで緑。画面での目視記録が無い） (2) **スクショ 390/1280px × 3画面**（B24 の恒久策待ち → **B33**。両幅の横あふれ0 は実測済みだが PNG の証拠が無い）。**この2件は Phase 7 で回収した**（(1) 実 Chromium で4タブすべてにクレジット4リンクを確認 = A12 (2) 合成データ27件で `docs/evidence/demo-*-{390,1280}.png` 8枚 = B33 done）。**Phase 6 の表の状態を `done` に直すかは `/release` の判断**。Phase 4/5 の積み残し（e2e手順13 の通し観測・実 iPhone 写真・実 HEIC・A11 の204件構成）も **B34** のまま Phase 7 へ
- 罠(Phase 6 で踏んだ): **`stats.test.ts` のスタイル分布が恒真だった**（入力を `STYLE_TERMS.map(...)` で作り期待値を実装と同じ出所から取っていた = **B15 と同型**）→ 入力を `STYLE_TERM_SAMPLE_SPECS: Record<StyleTerm,string>` でリテラル化し分布全体をリテラルで固定。**`Record` 型で網羅を強制したが `npm run ci` に `tsc` が無い**ので ci だけでは検出が働かない（→ **B36**）/ 実台帳の件数を実装に固定するのは `screens.test.tsx` の1箇所のみで **seed が無い CI では skip**（→ **B35**）/ 変異5件で赤を実演（排他バケツ→5赤 / note 混入→にごり4→5 / linkedWithoutChart→6赤 / 分母 records.length→13赤 / null ガード撤去→5赤）
- Phase 6 で起票: **B35**(実台帳の実測値を実装に固定する経路が CI では skip される) **B36**(`npm run ci` に `tsc` が含まれない) / 回収: **B29**(FlavorMap に分母を常設 → done) **B1(3)**(分母190 を画面で固定 → B1 は本文訂正のみ残る) / **未対応で持ち越し: B21 B23 B24 B25 B26 B30 B31 B32 B33 B34 B16 B5 B7**
- Phase 4/5 の結果(要約): `npm run ci` exit 0(29 files / 602 passed | 3 skipped) / 実 Chrome で **A8**(3000×4000・9.27MB → 300×400・12,896B / EXIF Orientation=6 が正立 / 上限超えは alert + `thumbnail:null`) **A11**(export 97,422B → `deleteDatabase` → import で3件すべて Blob・寸法一致) **A6/手順12**(`手動` バッジ5件・紐付けピル5値・取り込み内訳 191/7/5/**190**)。罠: **`src/index.css` の `img` 規則をレイヤー外に書くと Tailwind の utilities を必ず打ち負かす**(**jsdom は `css:false` なので永久に緑**) → `@layer base` に入れる / 恒真テスト2件 / trim 済みと生の `brandLabel` の比較。証拠は `docs/phases/PHASE_4.md` / `PHASE_5.md`
- Phase 3 の結果: `npm run ci` **exit 0**(21 files / **397 tests**) / **実 Chrome で `main ol > li` = 203**(空状態→取り込み→リロードでも203) / パネル内訳 紐付け186・未紐付け12・銘柄不明5・**フレーバー取得済み185** / export→全消し→import で203復元(**A11** サムネ無し版) / 赤の実演: key を `drankOn+brandLabel` に変異して**5テストが赤**。証拠は `docs/phases/PHASE_3.md` の「検証の証拠」
- 罠(Phase 3 で踏んだ): **StrictMode の擬似 unmount が `history.back()` を同期で呼び全オーバーレイが開いた瞬間に閉じた**(テストは全緑だった → dev で実ブラウザを触るまで見えない) / このシェルの `grep` は **該当があっても何も出さず exit 1**(否定形の検査が無検査で緑に見える → `command grep` を使う) / **実データのスクショは台帳の結合キーそのもの**なので `docs/evidence/phase3-390.png` `phase3-1280.png` は gitignore(B24)
- 罠(継続): `base` は `'./'` 固定 / `vitest.config.ts` は分離必須 / `100vh` 禁止 / 素の `npm install`・`npm test` は不許可 / `src/domain/` は React 非依存の純TS(**lint では未強制** → B21) / `import:sake-log` は **Node 22.18+ 必須・CI では走らない**(B23)
- 公開: https://one-shine.github.io/sake-record/ （public リポ。docs/ も追跡する。**飲酒台帳の本体は `data/seed/` と実データのスクショだけが gitignore**。コミットする射影は日付を持たない `linkBrand.cases.json`/`snap.json` と日付だけの `stats.cases.json` で、**2つに共通の列を1つも作らない**。全体ガードは `npm run ledger:check`）
- Phase 2 の結果: `npm run ci` 緑(6 files / 116 tests) / `import:sake-log` が **203件** で exit 0・冪等 / 紐付け **auto 173 + alias 13 = 186**・unlinked 12・unknown 5・チャートあり **185** / 素の完全一致 172本75種。証拠は `docs/phases/PHASE_2.md`

## 課題 / TODO

| ID | 種別 | 内容 | 優先 | 状態 | 出所 |
|----|------|------|------|------|------|
| B1 | improve | **SPEC 誤差5件の本文反映**: (1)「92%以上」→ 件数 ≥186（186/203=91.6% なので百分率だと落ちる）(2) e2e手順10 の分母 186→**185**（`ビキニ娘` id2020 は紐付くがチャート無し）(3) e2e手順12 の分母 191→**190** (4) 突合表「76種/173本」→ 素の完全一致は **75種/172本**（173本目 no.103 `翔空(Lagoon Brewery)` は括弧除去を経て一致）(5) **SPEC 本文2箇所（L51 / L165 のコメント）が「『高砂』はさけのわに5件」と書くが実測は4件**（2359静岡 / 9941三重 / 66006佐賀 / 77752島根）。**Phase 2 でテストとして固定できたのは (1)(2)(4)(5) の4件**: (1) `linkBrand.test.ts`「203本の紐付け内訳(A3)」が件数で assert、(2) 同「紐付け済み ≠ フレーバー取得済み」が 186→185 と欠けるのが `ビキニ娘` だけであることを固定、(4) 同「素の完全一致は 172本/75種」+ `翔空` の `brandName` を固定、(5) `tables.test.ts`「`高砂` は4件返る」と `linkBrand.test.ts`「同名4件の `高砂`」が4件を固定。**(3) の 190 は Phase 5 でテストとして固定した** — `src/integration/manualLink.test.tsx` が実データ203本に対し「取り込み直後 `withFlavor` 185 → 手動紐付け後 190 → 解除で 185」を assert（分母は取り込みパネルと同じ `summarize()` で数える）。実 Chrome でも取り込み内訳 191/7/5/**190** で観測した。**Phase 6 で画面での常設表示も済んだ**（`FlavorMap` に分母 185→190→185 を常設。B29 = done）ので、**残るのは SPEC 本文の訂正だけ**。あわせて PLAN の見出し「SPEC の誤差4件」も5件に追随させる | high | open | 計画時の実測 + Phase2 検証 |
| B2 | improve | **生成データの置き場所を `src/data/sakenowa/` → `public/data/sakenowa/` に変更**した逸脱を SPEC 本文に追随させる。理由: SW の原子的 SHELL に安定した既知パスを書けてオフライン保証が確実になる / 月次更新が JS バンドルのハッシュを変えない / 232KB の JSON を tsc の入力から外せる | high | open | PLAN 設計方針 |
| B3 | improve | **`linkBrand(label, prefecture)` は実装不能**（銘柄マスタとランタイムエイリアスを注入できず純関数にならない）→ `createLinker({brands,breweries,areas,aliases}) => (label,prefecture) => Result` に変更する。SPEC の型定義を訂正。**Phase 2 で実装**（`src/domain/linkBrand.ts` / 型は `types.ts` の `Linker` `LinkerTables`）。呼び出し側の形 `(label, prefecture) => Result` は SPEC のまま保った。「注入したテーブルだけを見る」11テストがリテラルのテーブルで純関数性を固定（索引を外に漏らさない / 既定の県に落とさない / 手動エイリアスが後勝ち）。**SPEC 本文の型定義の訂正は未了**（本文は B1 とまとめて直す） | high | done | 計画時の設計 |
| B4 | improve | **`SakeRecord` に2フィールド追加**: (a) `brandName: string \| null` — 紐付け時点の値を非正規化保存（毎回 brandId 逆引きすると テーブル非同期ロードを待つ / 上流から消えたら表示も消える / export が自己記述的でなくなる）(b) `sourceNo: number \| null` — `drankOn` は同日最大6〜7件重複し表裏ラベルの2組は内容で区別できないため、`drankOn` のみのソートでは**順序が非決定になり2本の赤武が入れ替わる**。**Phase 2 で `types.ts` に追加**（`SakeRecord.brandName` L33 / `sourceNo` L51）。`brandName` は「紐付いた186本すべてで非 null かつ銘柄マスタの名前と一致」をテストで固定し、`createLinker` は `alias`/`auto` で必ず埋める（上流から消えた銘柄を指すエイリアスは `alias` にせず名称一致へ進む = `brandId` だけ埋まる状態を作らない）。`sourceNo` の**実際の使用（並び順の決定性）は Phase 3 の永続化・Timeline で回収する** | high | done | 計画時の設計 |
| B5 | idea | **`flavorTags`(141) と `brandFlavorTags`(2970) を取得するがどの機能も使わない** — gzip 92KB のうち 28KB が機能ゼロの死重。記録詳細に味タグのチップ（ジューシー/フルーティ等）を出して Timeline のファセットにもするか、取得自体をやめるか判断する。**黙って同梱したままにしない** | med | open | 計画時の発見 |
| B6 | improve | **受け入れ基準の穴**: SPEC スコープ1の「編集・削除」とスコープ4の「検索/絞り込み」に受け入れ基準が無く、e2e も作成(手順13)しか通らない → PHASE_3 / PHASE_4 の完了条件に足した。SPEC 側にも基準を追加する。**Phase 3 で「検索/絞り込み」側は実装 + 実 Chrome で確認済み**（検索は銘柄/場所/メモの部分一致 / ファセットは 年7・都道府県35(県なし含む)・紐付け4 / 0件で「該当なし」を出し**全件へ戻さない**）。**Phase 4 で「編集・削除」側も実装 + テスト済み**（`App.test.tsx`「記録の編集と削除」4件 = 詳細から編集フォームが開いて更新される / 別の記録に切り替えても前の入力を持ち越さない / 削除は自作の確認ダイアログを経てから store に届く / 削除失敗で一覧から消さない。実 IDB の往復は `records.test.ts`。**実ブラウザでのクリック通しは未実施** → B34）。**残るのは (a) SPEC 本文への基準追加だけ** | med | open | 計画時の発見 |
| B7 | improve | **iOS Safari の7日間ストレージ退避**: SPEC は「サイトデータ削除で消える」しか警告していないが、インストールしていない Safari では7日間未使用で自動退避され得る。唯一のバックアップが手動 export である以上これは約束の穴 → `navigator.storage.persist()` 要求 + 初回に「ホーム画面に追加すると消えにくい」案内 + 経過日数督促（PHASE_7） | med | open | 計画時の発見 |
| B8 | improve | **飲酒アプリなのに年齢に関する表記が無い** → クレジットの隣に「20歳未満の飲酒は法律で禁止されています」を常設する。コストゼロで、無いと不注意に見える。**Phase 1 で実装済みだったのに open のまま残っていた**（`Attribution.tsx` L48 + `Attribution.test.tsx` が固定 / Phase 3 のスクショで 390px・1280px の両方でフッタに出ていることを確認） | med | done | 計画時の発見 |
| B9 | improve | **コミットバックからデプロイ到達までの実走確認**（PHASE_7）。`GITHUB_TOKEN` で push したコミットは他 workflow の `on: push` を再トリガしないため `deploy-pages.yml` に `on: workflow_call` を足して直接呼ぶ設計にした。コードベース初のコミットバックなので `workflow_dispatch` 手動実行で1回目で見る | med | open | 計画時の設計 |
| B10 | improve | **表示名 `saketime` こそが商標リスク**。SPEC は識別子（リポジトリ名・base・バンドルID）を守っているが、`saketime.jp` の SAKETIME株式会社は**同じ日本酒レビュー領域**なので混同が起きるのは表示名。`index.html` / `public/manifest.json` / `src/config/app.ts` の3ファイルに閉じ込め `check-naming.mjs` で強制し、改名が3ファイル操作で済む状態を保つ。**アセット作成前・ストア申請前なら改名はコストゼロ**（brain `niche-app-portfolio.md`「名前はまだタダのうちにクリアランスする」） | med | open | 計画時の発見 |
| B11 | improve | **エイリアス8件のうち2件は正規化に食われて冗長**（`髙砂/三重県` は異体字マップ+県で一意に絞れる / `ゆきのまゆ` はさけのわ名 `ゆきのまゆ（醸す森）` が括弧除去で一致）。安全網として保持するが、変異テストで「冗長であること」を固定し、正規化が変わったら気づける状態にする（PHASE_2） | low | open | 計画時の変異テスト |
| B12 | improve | `.claude/rules/testing.md` の frontmatter が HTML コメントの下にあり `paths:` スコープが効いていない / `.claude/hooks/format.sh` の case 節に `*.mjs` が無く `scripts/*.mjs` が整形対象外 | low | done | 計画時の発見 |
| B13 | bug | **さけのわ CDN は `accept-encoding` ごとに別世代のキャッシュ変種を返す。** 同時刻でも gzip 変種は age 33508s / weak etag / flavorCharts **1342**件、identity 変種は age 8861s / strong etag / **1344**件だった（Node の fetch は既定 gzip、curl は既定 identity なので手検証と食い違う）。放置すると月次更新が変種間で往復して無意味な差分を作り、データを古い世代へ巻き戻す → `accept-encoding: identity` に固定し、全行を id 昇順にソートして解決。**Phase 7 の月次ジョブでこの前提が効いているか確認する** | high | done | Phase1 実測 |
| B14 | bug | **オフラインで HTML だけ復元され JS/CSS が ERR_FAILED になる SW バグを踏んだ。** 原因2つ: (1) `caches.match` は既定で `Vary` を尊重し、`vite preview` が同一オリジンにも `Vary: Origin` を付けるため install 時の保存分とブラウザの subresource リクエストが突合しない (2) `.catch(() => cached)` が `undefined` に解決し得て `respondWith(undefined)` がネットワークエラーになる → `ignoreVary: true` + 明示的な 504 応答 + cache-first に変更して解決。当初「`Vary: Origin` は vite preview 由来なので本番では出ない罠」と考えたが、**公開後に実測したら GitHub Pages も `Vary: Accept-Encoding` を返していた**ため、Vary 突合の失敗は本番でも起こり得る。ヘッダを特定せず `ignoreVary` で一般に無効化したのが結果的に正しかった。Phase 7 の実機オフライン確認は公開URLで行う | high | done | Phase1 実測 |
| B15 | improve | **`Attribution.test.tsx` が恒真だった。** `toHaveAttribute('href', SAKENOWA_URL)` と config から import した定数を比較していたため、定数を `example.invalid` に書き換えても緑のままだった（赤の実演で発見）。ライセンス義務であって設定値ではないので URL をリテラルで書くよう修正。**他のテストでも「期待値を実装と同じ出所から取っていないか」を点検する** | med | done | Phase1 赤の実演 |
| B17 | improve | 一度 `docs/` と `.claude/` を gitignore にしたが、**正典（SPEC / PLAN / BACKLOG / PHASE）が版管理外になり、上書きしても戻せず・差分も追えず・`/phase-review` の `git diff` にフェーズ状態や課題の変更が出なくなる**ため公開追跡に戻した。docs に含まれるのは銘柄名・年別/県別の集計値までで、**日付・店名・備考は含まれない**（それらは `data/seed/` に閉じて gitignore 済み）。poker-gto も public で `CLAUDE.md` と `docs/` を追跡しており前例と揃う | med | done | Phase1 の公開方針 |
| B18 | bug | **コミットする2つの射影が結合できていた（台帳が復元された）。** `stats.cases.json` に `prefecture` を残したため、同じ203本の射影で県の多重集合が両側で必ず一致し、**両側で1件しかない9県が一意に突き合わさって「銘柄 × 日付」が確定**した（実測で9県。**この起票の初版は復元できた1件を実例として本文に書いてしまっていた**ので銘柄名と日付を落とした。B17 の「docs に日付は含まれない」を docs 自身が破っていた）。さらに `spec` が商品名経由で銘柄名を含む行が4行あり、結合を経ずに 銘柄 × 日付 が読めた。並び順を崩すだけでは防げない（値として結合キーが残る）→ **stats 側を日付文字列だけの配列に変更**し、県別集計(A10)は `linkBrand.cases.json` の県で検証する。射影の形自体を `linkBrand.test.ts` の「混入規則」でテスト化 | high | done | Phase2 検証 |
| B19 | improve | **「表裏ラベルの2組を dedupe しない」テストが CI で1度も走っていなかった。** 元 md（brain 側 = リポジトリ外）を読むスクリプトの自己検証しか無く、cases.json 側は日付を落としているので committed data からは検査不能だった → パースを `src/domain/parseSakeLog.ts` に切り出し、リテラル markdown の単体テストで守る（dedupe を入れる変異で5本が赤になることを確認）。スクリプトは I/O と 203件の確認だけの殻にして実装は1本に保つ | med | done | Phase2 検証 |
| B20 | improve | `linkBrand.ts` の「生の完全一致 → 正規化一致」の2段は**順序に意味が無い**（`exact ⊆ normalized` で、採用は1件のときだけなので入れ替えても出力が同じ）。実装コメントは「優先される」と書いていたが偽で、実際に効いているのは*段の存在*（同県で衝突する `丹沢山`/`丹澤山` は正規化一致だけなら曖昧）。コメントを訂正し、テストは「正規化で潰れた区別を生の表記が取り戻す」を実データ+リテラル注入テーブルの両方で固定した | low | done | Phase2 検証 |
| B21 | improve | **依存方向（brain の絶対ルール）が CI で強制されていない。** `src/domain/ ← src/store/ ← src/ui/` は今は手動 `grep` と「domain のテストを `@vitest-environment node` で回す」ことでしか守られていない。前者はコミット時に走らず、後者は `window`/`document` は捕まえるが **`import { useMemo } from 'react'` は node でも普通に動くので緑のまま通る**（`eslint.config.js` に `no-restricted-imports` の zone 設定が無い）。Phase 3 で `src/store/` が入ると逆流の機会が増える → eslint の `files: ['src/domain/**']` ブロックで `react` / `react-dom` / `../store/**` / `../ui/**` を `no-restricted-imports` で禁止し、**赤の実演（domain に `import 'react'` を足して `npm run lint` が落ちる）まで確認する**。**手 grep で代用できない理由がもう1つある**: macOS 標準 `grep` は `LANG=C.UTF-8` だと日本語を含む `.ts`/`.tsx` を「Binary file matches」と判定して行番号を出さないため、`command grep -a` を付け忘れた否定形の検査（「domain に react を import している行が無い」）が**無検査のまま緑に見える**（Phase 3 の検証で実際に踏んだ）。**さらに悪い罠を Phase 3 で実測した**: このハーネスのシェルでは `grep` が Claude Code のシェル関数（ugrep）に差し替わっており、**該当があっても何も出力せず exit 1 を返す**（`grep -c record src/ui/Timeline/Timeline.tsx` が空 / `command grep -c` は 25）。否定形の grep はこの環境では**常に「該当なし」に見える**ので、手 grep による依存方向の確認は手順として信用できない（`command grep` を明示すれば動く）。lint で強制する理由がもう1つ増えた。**Phase 7 で実装**: `eslint.config.js` の `no-restricted-imports` で domain ← data/store ← ui を宣言（domain の**テストだけ** `../data/` を許す。react と store/ui はテストでも禁止）。ルール自体が消えても気づけるよう `npm run boundaries:check`（`scripts/check-lint-boundaries.mjs`）が合成コード11件（逆流7 / 正しい向き4）で毎回自己検査し、`ci` に入っている。統合ステージの `npm run ci` で `lint` → `boundaries:check` の両方が緑 | med | done | Phase2 検証 |
| B22 | improve | **「飲酒台帳をコミットしていない」ガードが3ファイルにしか掛かっていない。** 日付混入を見ているのは `linkBrand.test.ts` の「コミットする射影の混入規則」4テストで、対象は `linkBrand.cases.json` / `linkBrand.snap.json` / `stats.cases.json` を名前で列挙している。`import-sake-log.mjs` の出力先を変える・別のスクリプトが `data/seed/` 外へ書く・docs に日付付きの実例を貼る、のいずれでも素通りする。**実際に素通りしていた**: B18 の起票文が復元できた1件を `県 / 銘柄 / 日付` の形で書いており、B17 の「docs に日付は含まれない」を docs 自身が破っていた（この更新で削除。ガードがあれば起票の時点で落ちていた）。→ `npm run invariants` に**リポジトリ全体**の検査を足す。**Phase 3 で実装**（`scripts/check-ledger-leak.mjs` / `npm run ledger:check`）。不変条件は「日付の数」ではなく**結合キーの同居**にした: 射影から読んだ台帳の日付(166種)と銘柄/都道府県(94種+県)が**同じ行に同居している追跡/未追跡ファイルが1つも無い**（日付だけ・銘柄だけなら射影として公開済みなので違反にしない / 検査器は値を持たず射影から読む / 失敗時の出力にも値を出さず場所だけ言う）。**実際に漏れていた20行を検出**: `parseSakeLog.test.ts` の fixture（日付+銘柄+県+備考の4行）・`parseSakeLog.ts` `types.ts` `import-sake-log.mjs` のコメントと定数・`docs/PLAN.md` `docs/BACKLOG.md` `docs/phases/PHASE_2.md` `docs/phases/PHASE_3.md` の説明文 → すべて合成値/構造の記述に置き換えた。あわせて `Timeline.test.tsx` の合成日付が偶然台帳の日付と衝突していた3行（同じ行に県名がある）も台帳範囲外の年に変えた。**残る穴**: 1文字の銘柄（`作` `梵` 等）は日本語の散文と衝突し得るので誤検出があり得る（現状0件）/ 日付だけを大量に溜めることは禁じていない | med | done | Phase2 検証 |
| B23 | improve | **`npm run import:sake-log` は Node の TS 型ストリップ（22.18+ / 23.6+）に依存し、CI では一度も走らない。** `scripts/import-sake-log.mjs` が `src/domain/parseSakeLog.ts` を直接 import している（パースを二重実装しないための選択で、これ自体は正しい）。結果として **(a) 実データ側の期待値「203件」「`(日付, 銘柄)` の重複が2組・各2件」を検査するのは手動実行時だけ**（Phase 3 でこの2組の期待値を日付・銘柄のリテラルから**構造の検査**に置き換えた = スクリプトも台帳の対を持たない）（元 md がリポジトリ外なので CI に持ち込めない。構造不変条件のほうは `parseSakeLog.test.ts` が守る = B19 で是正済み）、**(b) Node を 22.18 未満に下げると即失敗する**（失敗時は専用メッセージ + exit 1 を出すので黙って壊れはしない。実測で確認）。Phase 7 の月次更新ジョブで `import:sake-log` を回すなら、そのランナーの Node バージョンを固定するか `engines` に書く。**Phase 7 で実装**: `engines.node = ">=22.18.0 <23.0.0 \|\| >=23.6.0"`（23.0〜23.5 の穴は型ストリップの実態どおり意図的）。`npm run node:check`（`invariants` の先頭）が**走っている Node と `.github/workflows/*.yml` の `node-version` 3件の両方**を照合する。統合ステージの実測は Node v24.15.0 / ワークフロー3件とも `22` で緑（月次ジョブは `import:sake-log` を回さないので実行時の依存は無い） | low | done | Phase2 検証 |
| B24 | bug | **実データのスクリーンショットは台帳の結合キーそのもので、`ledger:check` の射程外。** Timeline の1行に (日付, 銘柄, 都道府県) が同時に写るため、`docs/evidence/*.png` を追跡すると B18 で「射影から復元できた」と問題にしたのと同じ組が public リポジトリに載る。**PNG は画素なので `scripts/check-ledger-leak.mjs`（テキスト検査）では検出できない** = 混入しても CI は緑。Phase 1 のスクショは記録0件だったので気づかなかった。→ 暫定対処: `docs/evidence/phase3-390.png` と `phase3-1280.png` を **`.gitignore` に明示列挙**し、台帳が写らない `phase3-390-empty.png`(空状態)だけ追跡した。**恒久策の判断が必要**: (a) 実データのスクショは常に gitignore し、追跡するのは合成データ/空状態のスクショだけにする（フェーズごとに列挙が増えるので `docs/evidence/` を既定 ignore + 安全な物だけ `!` で戻す形にする） (b) `ledger:check` に「`docs/evidence/` で新たに追跡される PNG があれば落とす」allowlist 検査を足す（画素は読めないので**ファイル単位のホワイトリスト**にしかできない）。**brain の品質バー「スクショを証拠にする」と「台帳を公開しない」が構造的に衝突している**ので、証拠の置き場（リポジトリ外の固定パス等）ごと決める。**Phase 7 で (a) と (b) の両方を実装した**: (a) `.gitignore` を `docs/evidence/*.png`（+ jpg/jpeg/webp）の**既定 ignore + `!` で個別に戻す**形に反転（フェーズごとに危険なファイルを列挙する形は「列挙し忘れた1枚」が入る）、(b) `check-ledger-leak.mjs` に2つの検査を追加 — index に載っている `docs/evidence/` の画像が `.gitignore` の `!` 行と一致すること（**allowlist の単一の出所は .gitignore。2箇所に列挙しない**）+ 実体の無い許可行を残さないこと。赤の実演: `git add -f docs/evidence/phase3-390.png` で1件の違反。**残る穴**: 許可済みの名前のまま中身を実データに差し替えると検出できない（画素は読めない = ファイル名 allowlist の原理的限界）。撮り直したら1枚ずつ目視する運用を `docs/evidence/README.md` に書いた | high | done | Phase3 検証 |
| B25 | improve | **`npm run ledger:check` の残る穴**（B22 で入れた全体ガードの限界。塞いだつもりにならないため列挙する）: (1) **1文字の銘柄**（`作` `梵` 等）は日本語の散文と衝突するので誤検出があり得る（現状の検出は0件） (2) **日付だけを大量に溜めることは禁じていない**（射影として公開済みなので違反にしないという判断。ただし日付203件の列と銘柄203件の列を別ファイルに置けば B18 と同じ復元ができる = ファイル単位でしか見ていない） (3) **行をまたいだ対は見ない**（同じ行に同居することだけを違反にしている。YAML/JSON で日付と銘柄が隣接行にある形は素通り） (4) **`git` が無い環境では例外で落ちる**（黙って通ることはないが CI 以外で使いにくい） (5) **バイナリは検査できない** → B24。(3) は「同一ファイル内の近接N行」に広げれば塞げるが誤検出が増えるので、実際に踏むまで広げない | med | open | Phase3 検証 |
| B26 | bug | **壊れたバックアップのプレビュー件数が生の行数のまま。** `src/ui/ImportExport/detectImportFile.ts:70` は `records` 配列の**長さ**をプレビューに出すので、中身が全滅（1件も wire 形として読めない）JSON でも「記録 2件」と表示する。取り込み実行側は `ok:false` で拒否し、**中身の無いストアの0件置き換え（全消し）は起きない**ので破壊はしないが、事前警告としては不正確（「2件読める」と言った直後に「0件しか読めない」と言う）。→ プレビューの件数を「wire 形として読める件数 / 全行数」に変える | med | open | Phase3 検証 |
| B27 | improve | **Stop hook を `verify.sh` → `verify-guard.sh` に差し替えた（由来: オーケストレーター。Phase 3 の実行中に入れたのでサブエージェントからは追跡できず「由来不明」と警告された。指摘自体は正しい）。** 理由: Phase 1 で有効化した `VERIFY_CMD='npm run check'` は、**並列エージェントが書いている途中のツリー（テストが先に書かれて実装が未着など）で必ず落ち、ターン終了を毎回ブロックする**。フックを外すと通常のターンで型エラーを抱えたまま終わる事故を捕まえられなくなるので、「ワークフロー進行中なら通し、停止中なら検証する」ガードにした。進行中の判定は `agent-*.jsonl` が直近3分に更新されているかで、ワークフローが終われば数分後に自動で検証が復活する（解除操作が不要）。3状態（進行中+赤=通す / 停止中+赤=exit 2 / 停止中+緑=通す）を実測で確認済み。**実装時の罠**: GNU の `-newermt` は macOS の find に無く、指定してもエラーにならず空を返すため常に「停止中」と誤判定する → `-mmin` を使う | med | done | Phase3 検証 |
| B28 | improve | **`manual` バッジだけ実データで1件も出ない**（手動紐付けは Phase 5 なので203本の内訳は auto 173 / alias 13 / unlinked 12 / unknown 5）。5種の描画は `Timeline.test.tsx` の単体テストが覆っているが、**実データの画面で確認できたのは4種**。絞り込みファセットも「存在する値だけ」を出す設計なので紐付けピルは4値。→ PHASE_5 の完了条件に「寫楽を手動紐付けしたあと `manual` バッジと5値目のピルが実データの画面に出る」を入れて回収する。**Phase 4/5 の修正ステージで PHASE_5.md の完了条件に明文化した**（それまで完了条件に無く、回収の指定が宙に浮いていた）。**回収済み**: (a) 実 Chrome で seed 203本を取り込み → 手動紐付け → 一覧に `手動` バッジ **5件**・紐付けピル **5値**・行の導線 17→12 を観測（`docs/phases/PHASE_5.md`「実 Chrome で通した手順12」）、(b) 自動化は `src/integration/manualLink.test.tsx` が `getAllByText('手動').length === 5` と絞り込みピル5値（`手動5` を含む）を assert し、`App` の `onChanged` を外すと赤になることを実演済み。**残る派生は B31**（5種のバッジとは別に「マスタから消えた」表示が Timeline / RecordDetail に無い） | low | done | Phase3 検証 |
| B29 | improve | **手動紐付けの直後に「フレーバー取得済み N」を同じ画面で見る導線が無い。** 分母を描いているのは `ImportExportPanel` の `SummaryView` だけで、**取り込みを実行した直後にしか出ない**。`LinkBrandPanel` は波及件数（「他N本にも適用した」）を出すが分母は出さず、Timeline / RecordDetail にも無い（FlavorMap は Phase 6）。そのため PHASE_5 の「検証」に書いてあった「**同一画面で**分母が 185 → 190 に増えるのを見る」は実ブラウザで満たせない。**数値そのものは 185 → 190 → 185 を実測済み**（(a) IDB の記録とチャート表の突合 (b) 書き出し → その JSON を取り込んで内訳「紐付け 191 / 未紐付け 7 / 銘柄不明 5 / フレーバー取得済み 190」を出させる、の2経路）。→ 暫定対処: PHASE_5 の検証手順を「取り込みの内訳で見る」に直した（Phase 4/5 の修正ステージ）。**Phase 6 で回収済み**: `FlavorMap` に分母を常設した — 実 Chrome で「**203本中185本(91%)**」＋内訳（未紐付12 / 銘柄不明5 / チャート無し1）が常時表示され、手動紐付け（寫楽5本 → 宮泉2401）で**同一画面の分母が 185 → 190** に増え、**reload 後も維持**され、解除で **185** に戻ることを観測した（推定で埋めず件数で併記）。自動化は `src/integration/screens.test.tsx`（実 seed。CI では skip → B35）と `src/domain/flavor.test.ts` + `src/ui/FlavorMap/FlavorMap.test.tsx` が固定し、変異M3（`linkedWithoutChart` を `unlinked` に寄せる）で6赤・M4（分母を `records.length` に）で13赤を実演。**B1 (3) の「画面で固定する」分もこれで閉じた**（B1 に残るのは SPEC 本文の訂正のみ） | med | done | Phase5 検証 |
| B30 | improve | **紐付け解除の非対称性が仕様として書かれていない。** `LinkBrandPanel` の解除は `auto` / `alias` の記録でも押せるが、**否定の別名（「この表記はこの銘柄ではない」）を持たない**ので、名称一致で紐付いた記録を解除しても**再取り込みで `auto` に戻る**。手動紐付け（`manual`）を解除した場合は別名も消えるので戻らない。`src/ui/LinkBrand/LinkBrandPanel.tsx` の実装コメントが「この差は BACKLOG 送り」と宣言していたのに起票が無く、正典（BACKLOG）から消える差分になっていた（Phase 4/5 の修正ステージで起票し、コメントから ID を指すようにした）。→ 判断が必要: (a) 否定の別名（拒否エントリ）を `aliases` に持たせ、再取り込みでも本人の解除を維持する (b) 解除の画面で「再取り込みすると戻る」と明記して仕様として受け入れる。**(b) は文言1行で済むが、A6「本人の判断が永続化する」と衝突する**（`auto` を否定した判断だけが消える） | med | open | Phase5 検証 |
| B31 | bug | **上流のマスタから消えた銘柄が「チャートが無いだけ」と見分けられない。** PHASE_5 のタスク3（「マスタから消えた」バッジ）が半分しか入っていない。満たしているのは「`sakenowaBrandId` を自動クリアしない」「非正規化した `brandName` で表示を継続する」「フレーバー分母から外れる」の3つで、名指しするのは `RecordForm` だけ（「さけのわのマスタに無い銘柄ID 999999」。`RecordForm.test.tsx` が固定）。**`Timeline` / `RecordDetail` にはバッジが無い**。とくに `src/ui/RecordDetail/RecordDetail.tsx:227` は `sakenowaBrandId !== null` だけを見て「さけのわにこの銘柄のフレーバーデータが無い。紐付け自体は済んでいる。」と言うので、**上流から消えた記録にも「紐付けは済んでいる」と言い続ける**（打てる手が違うのに同じ文になる = この関数自身のコメントが避けようとした誤読）。→ `tables.brandById.has(id)` を見て3値に分け、Timeline のバッジ表（`linkStatus.ts`）とは別の軸として出す（`linkStatus` を6値目に増やさない。バッジ表は5値のまま） | med | open | Phase5 検証 |
| B32 | improve | **Blob → object URL の hook が3箇所に写しで存在する。** `src/ui/PhotoPicker/thumbnailUrl.ts`（`useThumbnailImageRef`）/ `src/ui/Timeline/RecordCard.tsx:120` / `src/ui/RecordDetail/RecordDetail.tsx:285` が同型のコード（effect で `createObjectURL` → cleanup で `revoke` / `createObjectURL` が無い環境ではプレースホルダ）を各自持っている。Phase 4 で写真が実際に入るようになったので、**revoke 漏れを1箇所直しても他2箇所に残る**形になった（leak はタブを閉じるまで解放されない）。並列ステージが3ファイルを同時に触っていたため統合を避けた経緯。→ `thumbnailUrl.ts` に寄せて2箇所を差し替える（`importActions.ts` と `resize.ts` の `createObjectURL` は使い捨ての別用途なので対象外） | med | open | Phase4 検証 |
| B33 | improve | **実データのスクショが撮れず、Phase 4 と Phase 5 の完了条件「スクショ 390px / 1280px」が両方とも未達のまま done にした。** 原因は B24（実データのスクショは (日付, 銘柄, 都道府県) が同じ画面に写る = 台帳の結合キーそのもので、PNG は `ledger:check` の射程外）の恒久策が未決着なこと。brain の品質バー「390/1280 のスクショを証拠にする」と「台帳を公開しない」が正面衝突しており、**フェーズが進むほど未取得のスクショが積む**（Phase 3 は実データ2枚を gitignore、Phase 4/5 は撮影自体をしていない）。→ B24 の (a)/(b) を決めるのと同時に、**合成データ（架空酒 + 台帳範囲外の日付）を数件入れた状態のスクショを「追跡する証拠」の標準形にする**案を評価する（空状態のスクショだけでは情報密度・折り返し・写真付き行の体裁を証拠にできない）。**Phase 7 で採用して実行した**: `scripts/make-demo-backup.mjs`（`npm run demo:data`）が**アプリのバックアップ JSON そのもの**を27件ぶん決定的に生成し、それを**本番と同じ2段の取り込み**（`ImportExportPanel`）で入れた画面を 390/1280 で撮る。**`?demo=1` のようなアプリ側の導線は作らない** — 本番バンドルに1バイトも入らず、「実データを上書きしない保証」も既存の取り込み確認がそのまま担う。合成データの不変条件（`drankOn` が台帳の日付集合と交わらない / 銘柄が台帳の94種と一致しない）は生成時と `ledger:check` の両方が検査し、変異2件で赤を実演した。証拠は `docs/evidence/demo-{timeline,stats,flavor,area}-{390,1280}.png` の8枚（1枚ずつ目視済み）。**限界を明示**: 203行のスクロール・34区分198本の県別一覧の高さ・延べ314の棒グラフの潰れは**合成27件では出ない**ので、実データ側の証拠は従来どおり DOM 実測（件数・`scrollWidth - clientWidth`）で取り、スクショにはしない。分担と手順は `docs/evidence/README.md` | med | done | Phase4/5 検証 |
| B34 | improve | **実機・実ファイル・通し観測の積み残し（Phase 4/5 で done にしたが測っていないもの）。** (1) **e2e手順13 の通し観測が無い** — 写真→サムネイル→保存→一覧は実 Chrome、候補を選ぶと県・蔵元・6軸が埋まるのは合成テーブルの単体テスト、`紀土` が 819/和歌山県/チャートありは `tables.test.ts` と、3つに分かれている (2) **EXIF 回転は合成 JPEG に APP1 を自前注入したファイルで確認**しており、iPhone が撮った写真は1枚も通していない (3) **実 HEIC ファイルを投入した run が無い**（HEIC 専用の案内文は単体テストのみ。Chrome は HEIC をデコードできないので実測可能なはず） (4) **A11 の「写真付き1件を含む204件」構成は未実行**（実測は写真3件の往復 + Phase 3 の203件） (5) **編集・削除を実ブラウザでクリックして通した run が無い**（jsdom の `App.test.tsx` は store を `vi.mock` している）。→ (1)(4)(5) は Phase 6 のブラウザ検証で seed + 写真1件の状態を作れば同時に潰せる**はずだったが、Phase 6 の検証は seed 203本だけで写真を1件も入れておらず、編集・削除のクリックも通していない**（Phase 6 で駆動したのは統計 / 味 / 産地 / 一覧 と手動紐付け）。**5項目すべて未回収のまま Phase 7 へ持ち越す**。(2)(3) は実機（Phase 7 / A15 A16）で回収する。**Phase 7 の統合ステージで (1)(4)(5) を回収**（実 Chromium / `vite preview` の本番ビルド）: (1) 写真(1280×800 JPEG)を選ぶ→サムネイル 400×250 / 8,073バイト→`紀土` をサジェストから選ぶ→和歌山県・平和酒造・6軸(45/50/22/38/28/53)が自動で入る→評価4・場所「自宅」で保存→一覧の先頭に出る、を**1本で通した** (4) seed 203 + 写真付き1件 = **204件を書き出し(86,940バイト) → `indexedDB.deleteDatabase('sake-record')` → 再読込で0件 → 取り込みで204件 + エイリアス1件が復元**、サムネイルは Blob 8,073バイトで一致し `<img>` が 400×250 で描画された (5) 詳細→編集→場所を書いて保存→詳細に戻る / 詳細→削除→**自作の確認ダイアログ**（OS の `confirm()` ではない）→0件、を実ブラウザでクリックして通した。**残るのは (2) 実 iPhone 写真の EXIF 回転 と (3) 実 HEIC ファイルの2つだけ**（どちらも実機が要る = e2e手順15 と同じステージ） | med | open | Phase4/5 検証 |
| B35 | improve | **実台帳（203本）の実測値を実装に対して固定するテストが CI では1本も走らない。** 固定しているのは `src/integration/screens.test.tsx` の1ファイルだけで、`data/seed/` を要するため CI では **6 tests すべて skip**（seed を退避して実測: 1 file / 6 tests skipped ＋ stderr に SKIP 1行。`integration` 全体では seed 有り21 passed / 退避で19 skip）。したがって **203 / 65 / 福島22 / 純米112 / 延べ314 / 185 / 190 / 未進出14** といった数値は、リポジトリだけを見る CI では守られていない（守られるのは*規則*まで = `stats.cases.json` に対する集計テストと、リテラル入力の分布テスト）。関連して **`EXPECTED_STYLE_COUNTS` を `computeStats` の出力と突き合わせる恒久策が無い**: 突合には203本の**スペック列**が必要だが、それは公開している射影2ファイルに無く、privacy 規則（B18 / B22）により新しい fixture にも書き出せない。現状は `src/domain/stats.ts:35-48` と `EXPECTED_STYLE_COUNTS` の doc コメントに「何を守っていて何を守っていないか」を明記して据え置き、突合はブラウザ検証で行っている（Phase 6 で実施）。→ 判断が必要: (a) スペック列を**不可逆に変換した**射影（語の有無だけの bit 列など。日付も銘柄も持たない）を1本追加して CI で突合できるようにする (b) 現状（doc + フェーズごとのブラウザ検証）を仕様として受け入れる。**skip は stderr に出るので無音ではない**点は現状の救い | med | open | Phase6 検証 |
| B36 | improve | **`npm run ci` に `tsc` が含まれない。** `ci` は `invariants`(data:check / naming:check / ledger:check) → `lint` → `build` → `attribution:check` → `test` で、型検査は `npm run check`(`tsc -b && eslint .`) 側にある。`build` が `tsc -b && vite build` なので**結果的には型エラーで落ちる**が、`ci` の意図としては型検査が独立の門になっていない。Phase 6 で `STYLE_TERM_SAMPLE_SPECS: Record<StyleTerm, string>` を入れて「スタイル語の追加・改名はコンパイルで落ちる」網羅強制を作ったので、**この検出が効くかどうかが `build` の副作用に依存している**のは明示しておくべき状態。→ 判断が必要: (a) `ci` に `check` を足す（`lint` が二重に走るので `ci` 側を `tsc -b` だけにする等の整理が必要） (b) `build` が `tsc -b` を含むことを根拠に現状維持と決め、`package.json` にコメントで書く | low | open | Phase6 検証 |
| B37 | bug | **削除の確認ダイアログが銘柄の無い記録で「」と空の鉤括弧を出す。** `src/ui/RecordDetail/RecordDetail.tsx:89` の `title = record.brandName ?? record.brandLabel` は、銘柄不明（`brandLabel` が空文字）の記録では**空文字**になり、L204 の確認文が「2026年7月26日の「」を削除する。取り消せない。」になる（Phase 7 の統合ステージで実ブラウザの削除を通したときに観測）。**取り消せない操作の確認文で対象が空**なのは、押す前に何を消すか確かめられないということ。`??` は `null` しか拾わないので空文字が素通りする（`brandName` が `null` の `unknown` 記録は必ずこの経路）→ 空なら「銘柄不明の記録」等に落とす。Timeline のカードは同じ状況で「銘柄不明」を出しているので、**同じ状態が画面ごとに別の見え方**になっている | med | open | Phase7 統合検証 |
| B38 | bug | **SW の「明示的な失敗応答」が反転していて、オフライン未キャッシュが素のネットワークエラーになっていた。** `public/sw.js` の `offlineFailure()` が `new Response('', { status: 504, statusText: 'オフラインでキャッシュにも無い' })` を返していたが、`statusText` は **ByteString(ISO-8859-1)** なので日本語を入れると **Response の構築自体が TypeError**（実測: `Cannot convert argument to a ByteString because the character at index 0 has a value of 12458`）。結果 `respondWith` に渡した promise が reject し、**この関数が避けようとしていた状態そのもの**（実測: preview を止めた Chromium で `fetch('./data/sakenowa/__missing__.json')` が 504 ではなく `Failed to fetch` / `net::ERR_FAILED`）になっていた。`src/data/tables.ts` の `if (!res.ok) throw new Error('さけのわデータを取得できない: … (${res.status})')` も到達不能。**直した**（理由の日本語は本文に置き `statusText` は `'Gateway Timeout'`。修正後の実測は status 504 / statusText `Gateway Timeout` / body「オフラインでキャッシュにも無い」）。根の原因は**この関数を覆うテストが1本も無かったこと**なので `src/pwa/sw.test.ts`（13件）を追加 — 出荷される `public/sw.js` を `?raw` で読み、ビルドと同じ2箇所のプレースホルダを置換して `self`/`caches`/`fetch` だけ差し替えて評価する（`Response` は Node のまま = 実ブラウザと同じ ByteString 検査が効く）。install の原子性 / activate の旧世代削除 / オフライン未キャッシュの 504 / ナビゲーションのシェルフォールバック / cache-first / `ignoreVary` / 素通し（クロスオリジン・非GET）を固定。**修正を外すと2件が赤、戻すと13/13緑**を確認済み | high | done | Phase7 実機検証 |
| B39 | improve | **`sw.js` の `cache.put` が `res.ok` だけを見ているので、206 と `Vary: *` の応答で reject する。** Cache API は仕様上 status 206 の Response と `Vary: *` を持つ Response を `put` で受け付けず TypeError を返すが、`public/sw.js:90` の条件は `res.ok`（200〜299）なので 206 が通ってしまう。`void cache.put(...)` で待っていないため**未処理の rejection になるだけで応答は返る**（実害は SW のログだけ）。**未観測**: このアプリは同一オリジンに Range 要求を出す経路（`<video>`/`<audio>`/大きな画像の分割取得）を持たず、写真は IndexedDB の Blob = `blob:` URL で SW の射程外なので、今のところ発火しない。→ 判断が必要: (a) 条件を `res.status === 200` に絞る（Workbox の既定と同じ。発火しない分岐を増やすことになる） (b) 発火経路が無いことを根拠に据え置き、`<video>` 等を入れるときに直す | low | open | Phase7 SW テスト整備時に仕様から気づいた |
| B40 | bug | **月次更新ジョブの2つの穴（`update-sakenowa.yml`）。** (1) **cron `17 20 1 * *` は JST では毎月2日 05:17 に走る**（UTC 指定なので毎月1日にはできない。「毎月1日に更新」と読める記述と実際の起動日がずれる → 意図なら「JST 2日」と書き、1日が要件なら前月末日の UTC 指定にする） (2) **コミットの前に通しているのは `data:check` と `test` だけで、`tsc` / `lint` / `build` を回していない**（データだけの変更なら型もビルドも壊れないという前提だが、`data:check` は JSON の形しか見ないので、たとえば新しい行が型と食い違う形で来た場合に**壊れたデータが main に入って自動デプロイまで走る**）。PHASE_7 の設計原則「検査をコミットの前に置く」を自分の作った順序が部分的に満たしていない | med | open | Phase7 前ステージからの申し送り |
| B41 | improve | **上流の件数を固定したテストがあるので、さけのわが1件増えるだけで月次ジョブが赤で止まり自動更新が通らない。** `tables.test.ts` / `linkBrand.test.ts` が brands 3264 などの件数をリテラルで持ち（回帰検出としては正しい）、`update-sakenowa.yml` は `test` をコミット前に置いているので、**上流の正当な増加でジョブが失敗し自動コミットに到達しない**。設計として明記済み（黙って古いデータで緑になるより赤で止まるほうが良い）だが、**運用としては毎月人手で期待値を更新することになる**ので、(a) 件数は下限（`>= 3264`）にして厳密一致は別の「上流スナップショット更新」テストに寄せる (b) 現状維持で「月次ジョブが赤 = 期待値更新の合図」という運用を `docs/` に書く のどちらかを決める | med | open | Phase7 前ステージからの申し送り |
| B42 | improve | **PHASE_7 のタスク2件が未着手**: (1) `public/screenshots/mobile-1.png` / `desktop-1.png` と `public/manifest.json` の `screenshots` フィールド（実ブラウザで manifest に `screenshots` が無いことを確認。無くてもインストールは可能だが、インストールプロンプトのリッチ表示が出ない） (2) `README.md`（リポジトリは public で公開中なのにトップに説明が無い）。**スクショは実データを写せない**（B24）ので、B33 で用意した合成データ（`npm run demo:data` → 2段の取り込み）で撮る | med | open | Phase7 |
| B16 | idea | 1280px 幅で下端タブが全幅に伸び、本文も左端に寄る。モバイル先行の PWA として許容範囲だが、PC で見たときの体裁として本文に max-width とタブの中央寄せを検討する（Phase 3 で Timeline を作るときに実データで判断する）。**Phase 3 で本文側は解決**（`max-w-3xl` = 768px で中央寄せ。1280px のスクショで確認）。**残るのは下端タブバーが全幅に伸びる件だけ** — 4タブが1280px に散って間隔が空く。タブ側に同じ max-width を当てるかは Phase 6(4画面が揃う)で実データで判断する予定だったが、**Phase 6 では判断していない**（実 Chrome で 390/1280 の両幅の**横あふれ0**は確認したが、体裁の判断は 1280px のスクショが要り、それが B24/B33 で撮れていない）→ Phase 7 のリリース確認に持ち越す。**Phase 7 で 1280px の実 Chromium で「下端タブが全幅1280に伸びる」ことを再確認したが直していない**（合成データのスクショ `docs/evidence/demo-*-1280.png` が撮れたので体裁の判断はできる状態になった）→ `/release` で判断する | low | open | Phase1 スクショ |

## 完了
<!-- done にした項目をここへ移す(履歴)。 -->
