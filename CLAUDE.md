# CLAUDE.md

## 概要

- 日本酒の飲酒記録アプリ。2020-12 から Excel + 写真で溜めた203本を置き換える。React 19 + TypeScript + Vite の SPA / PWA。UI は日本語。
- **中核の分担: 味のデータは自分で入力しない。** 銘柄に紐づくフレーバー6軸は外部（さけのわデータ）から取り、本人が入れるのは評価とメモだけ。飲みながらの入力を最短にするための設計判断で、これが崩れると続かない。
- 保存の第一は IndexedDB（端末内）。**端末間の同期は本人の同期先にだけ置く形で入れる**（2026-08-01 に「端末内に閉じる」をやめた → B69 / `docs/phases/PHASE_8.md`）。**恒久的に残るのは「第三者のサービスに預けない」**のほうで、クラウド OCR / AI API / 解析サービスは変わらず使わない（端末内 OCR の根拠がここに乗っている）。**他人との共有・複数利用者は恒久的にスコープ外。** 同期を設定していない端末でも全機能が動く（同期は足すもので前提にしない）。JSON エクスポート/インポートはやめない。
- 公開: GitHub Pages（public リポジトリ + `noindex`）。**個人の飲酒記録そのものはコミットしない**（`data/seed/` は gitignore）。

## コマンド

```bash
# dev:        npm run dev        (http://localhost:5173/ — base は './' なのでルート配信)
# build:      npm run build      (tsc -b && vite build && inject-sw-precache)
# test (all): npm run test
# test (one): npm test -- linkBrand      (素の `npm test` は許可設定でブロックされるので引数を付ける)
# check:      npm run check      (tsc -b && eslint .)
# 全部:       npm run ci         (invariants → lint → build → attribution:check → endpoints:check → test。CI と同一定義)

# データ:     npm run fetch:sakenowa     さけのわ6endpointを取得 → public/data/sakenowa/*.json
#             npm run fetch:readings     銘柄名の漢字の読みを書き出す → public/data/kanji/readings.json
#             npm run data:check         同梱データ(public/data/ 全体)の gzip 合計 ≤200KB を検証
# 不変条件:   npm run naming:check       ブランド名の出現を表示文字列3ファイルに限定 + base が './'
#             npm run attribution:check  dist にクレジット3件(さけのわ/地図/読み) + noindex があるか
#             npm run endpoints:check    dist の URL のホストが全部「理由付きの表」に載っているか(A30)
# OCR:        npm run ocr:assets         public/ocr/ の同梱物(wasm/worker/学習データ)を作り直す
#             npm run ocr:check          同梱物の sha256・合計サイズ ≤12MB・dist への到達を検証

# 同期サーバ(server/ は独立した package。アプリの依存は増やさない):
#   npm --prefix server run dev            wrangler dev(ローカル D1。http://localhost:8787)
#   npm --prefix server run verify         受け入れ検査40件(認証/CORS/勝ち負け/写真/分割)
#   npm --prefix server run check          tsc(アプリの `tsc -b` には入れない)
#   npm --prefix server run schema:local   ローカル D1 にスキーマを流す
#   npm --prefix server run schema:remote  本番 D1 にスキーマを流す
#   npm --prefix server run deploy         wrangler deploy
#   ※ 版を上げたときのデプロイ順は schema:remote → deploy → git push(アプリ)。
#     アプリを先に出すと、まだ古いサーバが新しい版を断って**記録の同期ごと止まる**
```

## コードスタイル

- コメントは**自明でない「なぜ」**にだけ書く。何をしているかの再説明は書かない。
- UI コピーは**常体**で統一する（敬体と混ぜない）。
- 絵文字をアイコン代わりに使わない。アイコンは `src/ui/icons/icons.tsx` の自作ライン（24グリッド / stroke 1.5 / currentColor）で統一する。
- 情報密度と論理を優先する。見出し下の飾り罫、全角のカラーバー、本文の中央寄せ、要求していないクリーム/ベージュ背景は入れない。
- **日本語ラベルは語中で折れる。** 直すときは必ず対で: コンテナに `flex-wrap` + `gap-y-*`、短い原子ラベル（バッジ・ピル）に `whitespace-nowrap`。
- OS 既定の `confirm()` / `<select>` / `<input type="date">` を使わない（自作する）。
- **配色は `src/index.css` の `@theme` の意味的トークンから引く**（`bg-canvas` `text-ink` `border-line` など）。`bg-stone-800` のような**スケール値を直書きしない** — トークンが配色の**単一の出所**で、将来ダークを足すなら値の差し替えだけで済む状態を保つ。旧→新の対応表は `src/index.css` 末尾のコメント節。
- **Tailwind のクラスを文字列連結で作らない。** `fill-amber-${n}` のような書き方は静的抽出が候補を見つけられず**本番で色が消える**。必ずリテラルで書く（段の切り替えは配列にリテラルを並べる）。
- **連続量のランプは白背景なので「多い＝濃い」。** ダーク前提の「多い＝明るい」を持ち込まない（地図の塗り・棒・凡例・文言のすべてで向きを揃える）。
- **地と面はクリーム/ベージュに寄せない。** 白〜ごく薄い中性グレー（`zinc` 系）で、暖色寄りの `stone` は使わない（`stone-50` はクリームに見える）。テーマはライト単色で `prefers-color-scheme` による自動切替を入れない（`html` に `color-scheme: light`）。

## アーキテクチャ

- **依存方向は一方向に固定する: `src/domain/` ← `src/store/` ← `src/ui/`。** `src/domain/` は React 非依存の純TS で、`react` / `window` / `document` / `process` を import しない。外部依存なしで単体テストできる状態を保つ。
- `src/domain/linkBrand.ts` が**銘柄紐付けの唯一の実装**。`scripts/` 側は markdown → 行JSON の射影だけを行い、紐付けも集計も持たない（暗黙の二重実装は必ずドリフトする）。集計も同じ規律で `src/domain/stats.ts` のみ。
- `linkBrand` は純関数にするため `createLinker(tables) => (label, prefecture) => Result` の形を取る（銘柄マスタとランタイムエイリアスを注入する）。
- **ルックアップのキーが定義域外のとき、結果が「全件」にフォールバックしてはならない。** 空を返す。銘柄サジェストと都道府県絞り込みの両方に効く（フォールバックすると別県の同名銘柄に誤紐付けする）。
- **紐付け済み ≠ フレーバー取得済み**（紐付いてもチャートが無い銘柄がある）。6軸集計の分母は「フレーバー取得済み件数」を出し、`unlinked`/`unknown` に推定値を埋めない。`linkStatus` のバッジ対応表は1箇所に集約する。
- **味タグは「その語が付く銘柄が少ない順」に並べる（B76。`src/domain/flavorProfile.ts`）。** さけのわの並びのままだと**どの銘柄を開いても先頭が 酸味・辛口・旨味**になる（2,136銘柄の半数以上に付く語なので銘柄を区別しない）。並べ替えると銘柄の顔になる（台帳79銘柄の希少上位5語は77件が一意）。**出現数は `decodeFlavorTags` が実データから数える**（`maxTagsPerBrand` と同じくリテラルで持たない）。**数えられなかった語は 0 で埋めず末尾に置く** — 0 は「最も希少」の意味になり、未知の語がその銘柄の一番の特徴として先頭に立つ。**並べ替えたら件数と分母を画面に出す**（並びの理由を読み手が説明できない状態にしない）。
- **読みの門は入口ごとに変える。** `search(かな)` は**人が打った**文字列なので門を置かない（打ち途中の `きゅ` で `宮` に当たる）。`find(文字列)` は**OCR が読んだ**雑音の海なので**5文字以上の読みしか採らない**（実測。2文字だと銘柄35〜129件が候補になり、4文字でも誤りが1件残る。5文字では実写真5枚で誤り0）。**読みで当たった候補は字の候補より先に出す**（読み全体が5文字そろう確率は、字が散らばって当たる確率よりずっと低い）。**ただし完全一致は要求しない — 雑音1文字の割り込みを許す**（同じ写真・同じ実装で卓上は `みやいずみ`、実機 iOS は `みやゃいずみ` と読んだ。実測で誤りは増えず、2文字に緩めても到達は増えない）。**読み飛ばした分は読みの長さに数えない**（雑音で門を通せてしまう）。読み表が取れなくても**記録は作れる**（`loadTables` はこの1本の失敗だけ飲み込む）。
- **OCR は「銘柄サジェストの補助」であって銘柄を確定しない。** 候補を出すだけで、選ぶのは本人（選択は手動サジェストと同一の経路に合流させる）。読めない・確度が足りないときは**もっともらしい別銘柄を上位に出さず**「読み取れなかった → 手で選ぶ」に落とす。**手動サジェストの経路を常に残す**（OCR 資産の取得失敗・SIMD 非対応端末を含む）。**外したときの助言は「まだ試していないこと」だけ言い、失敗欄から「一覧から選ぶ」を直接開けるようにする**（囲んだ人に「囲むと読みやすい」と返さない）。`unlinked` に推定値を埋めないのと同じ規律。
- **銘柄に辿り着く道は3本ある: 打って探す（`createSuggester`）/ 写真から絞る（OCR）/ 一覧から選ぶ（`createBrandBrowser` の 県→蔵元→銘柄）。** **かなでも引ける**（B68。`きど` → `紀土`）— さけのわは読みを持たないので、KANJIDIC 由来の**漢字1字ごとの読み**を同梱し、銘柄名を読みに分解して照合する（`src/domain/reading.ts`）。**読みは展開しない**（3264銘柄で409,875通りになる）。**ローマ字はまだ引けない**（`KID` では届かない）。3本目はそこと OCR の空振りを同時に埋めるもので、**ラベルから確実に読めるのは蔵元名と都道府県**という前提に立つ。どの道から選んでも `handlePick` の1経路に合流させる。
- **候補の門は「希少性 → 被覆率 → 近接 → 字種」の4段。** 文字を**集合**としてしか見ないと、読めた文字が多いほど「たまたま全部含む」銘柄が増える（実機の異なり字87で `Bange Yamada Hanjukunama` が12字中12字の全字一致を主張した）。**当たった字が1か所に固まっていること**を要求する（`minimumWindow`。塊＝パスをまたいだ寄せ集めは近接の証拠にしない）。**1字の銘柄名は候補にしない**（全字一致が定義上つねに成立するので被覆率でも近接でも切れず、80字超の読みでは稀な1字がほぼ確実に現れる。実機で `回` `作` が並んだ）。**ラテンだけの銘柄名は綴りそのものを要求し、3字以下は候補にしない** — 字種が26しかないので連続したラテンの誤読が短い名前をほぼ必ず覆う。
- **精度が足りないときに緩めるのは候補の門ではなく「人が押す道」。** 候補の閾値（希少性・被覆率）を下げると増えるのは当たりではなく当てずっぽう（実測で否定済み）。代わりに**読めた字を絞り込みの鍵として押せる形で出す**（`createCharNarrower` → 押した先は手動サジェストと同じ一覧）。鍵は**信頼度で照合から外した分も含めた読めた全部**から作る（押すのは人なので勝手に1位が出ない）。実測 9枚で候補 2 + 1タップ 4 = 6枚に到達。**受け皿ができたので候補の門は逆に締めた**（被覆率 1/2 → 2/3。1/2 だと4字の銘柄が雑音2字で通り、実物の `宮泉` で `ビキニ娘` が出た）。
- **OCR の律速は認識ではなく検出・分割。** 銘柄の列だけを正しく切り出せば `jpn` PSM6 で 12/19 字・4/9 銘柄まで読める（倍率には鈍感で、文字高の正規化は梃子にならない）。**テキスト行の検出をヒューリスティクスで自作すると逆に悪化する**（列の連結成分で分割 → 4/19。実測）。tesseract は**文書OCR**で、曲面・斜め・装飾書体のラベルは研究上**シーンテキスト認識**という別問題。**ただし engine を載せ替えても到達は増えない**（2026-07-31 に PP-OCRv4 を実ラベル5枚で実測。到達は現行と同じ 1/5）。**実ラベルで読めない銘柄名は装飾書体（筆・草書・角印）で、学習データの外だからどの engine でも読めない** — 縦書きの並べ替えも列のまとめ直しも原寸の切り出しも 0/5 で変わらなかった。載せ替えで買えるのは速さ（9〜28秒 → 1.3〜3.9秒）と静けさ（読み取り 403〜1,620字 → 2〜18字）だけで、資産 7.7MB → 25〜30MB（SPEC の上限12MB超）に見合わない（→ B67）。**装飾書体は「一覧から選ぶ」経路と読み（B68）が受け皿**で、到達を伸ばす筋は engine ではない。**実測: 読みを入れて 1/5 → 2/5**（`宮泉` のふりがな `みやいずみ` から候補が出る）。ローマ字（`HASSEN` `KID`）は PP-OCR なら正読するが、現行 engine では読めないので上積みを測れない。
- **文字の場所は「自動で提案 → 外れたら人が囲む」の2段。** tesseract のレイアウト解析は画面いっぱいの文書が前提で、**瓶の全体が写る写真からラベルを見つけられない**（瓶全体の合成9枚で到達 4/9。実機では「全く読み取れない」ように見える）。`src/lib/ocr/findLabel.ts` が**強い縁を持つ画素の割合が高いセルの最大連結成分**でラベルらしい範囲を提案し、切り出して読む。**平均勾配ではない** — 平均は石の天板・布・木目のような細かい質感に負ける（実機写真で天板を掴んでラベルを完全に外した）。**候補が出なければ全体も読んで合算**（悪化を構造的に防ぐ）。これが「銘柄を推測しない」規律と衝突しないのは**場所の提案であって内容の判定ではない**から — どの経路でも候補の門と選ぶ人は同じで、読んだ範囲は画面に明示し、枠は「ラベルを囲んで読み取る」で人が引き直せる（自動の枠が初期値に入る）。囲んだ範囲は原寸から切り出し（`cropImage.ts`。枠は表示比率 0..1 で受け渡す）同じ認識経路に流す。**手動の切り出しに失敗したら全体に黙って落とさない**（枠が効いていないように見える）。実測（ボタン1回）: シーン 4/9→**8/9** / 写真ふう 6/9→**7/9** / clean 7/9→**8/9**。**装飾書体（篆書・勘亭流など）の銘柄名は囲んでも前処理でも読めない** — 学習データの外なので、そこは一覧から選ぶ経路に落とす。
- **紙の質感は局所適応二値化で落としてから渡す**（`src/lib/ocr/binarize.ts`）。tesseract 自前の大域二値化では**紙のリブ（畝）・繊維・照明むら**が消えず、実機でラベル20〜30字に対し読み取りが200字を超えた（中身は `ー` と `ニ二三` = リブを長音符として読んでいた）。**掛けるのは中間調が5%以上の画像だけ**（既に白黒に近い画像に掛けると読み取りが落ちる。実測 8/9→6/9）。**比だけでなく絶対差（24階調）も要求する** — 比だけだと暗い場所やぼけで地の全面がインクになる（実測でインク率21.7%）。
- **OCR には原寸の元ファイルを長辺2000pxに縮めて渡す**（長辺400px のサムネイルでは解像度が足りず、原寸の4000px前後は大きすぎて逆に読めない。実測 3/9→6/9・3.9s→1.1s）。縮小は `src/lib/ocr/prepareImage.ts` で、失敗したら元ファイルをそのまま返す（投げない）。原本は `RecordForm` の state にしか置かず、保存も dirty 判定もしない。**サムネイルの仕様（長辺400px / 50KB以下）は変えない**。
- **サムネイルは Blob ではなくバイト列（`ArrayBuffer`）で保存する（B72）。** `Blob` は structured clone でも**参照**として IndexedDB に入り、iOS の WebKit がその実体を後から回収する（`size` は残るのに中身が読めなくなる。実機で1件失った）。`ArrayBuffer` は**値として複製される**のでこの経路が構造的に無い。**Blob は表示の直前にだけ組む**（`src/ui/common/thumbnailUrl.ts` の1箇所）。MIME は値に添えず `THUMBNAIL_MIME`（domain の定数）を引く — `resize.ts` が JPEG 以外を保存させないため。**保存形の詰め替えを `onupgradeneeded` でやらない** — `blob.arrayBuffer()` が非同期で、version change transaction は await を挟んだ時点で自動コミットされ以降の書き込みが無音で落ちる。版上げは器だけ、詰め替えは接続後（`src/store/migrateThumbnails.ts`）。**移行で更新時刻を動かさない**（動かすと全件が「いま編集した」ことになり、次の同期で別端末の編集を倒す）。**移行で読めなかった写真は `meta` の取り直し待ち行列に積む** — 積まないと、同期先に良い複製が在るのに二度と取りに行かない。
- **OCR エンジンは `src/lib/ocr/`、照合は `src/domain/brandFromText.ts`。** エンジンはブラウザ API（Worker / wasm）依存なので domain に置けない。照合は純関数として domain に置く（**eslint の境界 regex に `lib` が無いので機械強制されていない** → B54）。
- **OCR 資産は同一オリジン（`public/ocr/`）に同梱する。** 写真を端末外に出さないのが要件なのでクラウド OCR / 第三者 CDN を使わない。**7.7MB を SW の原子的シェル（`addAll`）に入れない**（install ごと落ちる）。別のキャッシュ層に置き、初回は「OCR を使う」明示操作の後にだけ取得する。エンジン本体は**動的 import** で遅延チャンクに留める（静的 import にすると初期バンドルに 3.8MB が乗る。ガードが落ちることを実演済み）。
- **画面は5タブ（記録 / 統計 / 味 / 産地 / 知る）。** 「知る」（`src/ui/Learn/`）は基礎知識と出典/ライセンスの置き場で、**記録に依存しない**（props で受け取るのは開く下位タブ `initialPanel` だけ）。**「知る」の中はさらに6つの下位タブ**（種類 / ラベル / 季節 / 産地 / 味 / アプリ）で、1画面に1トピックだけ出す。**タブは読む人の関心で割る**（「数え方」のような実装の都合で割らない）。**説明は平易さを優先する**（個人利用のアプリなので法令上の厳密さは求めない = 出所バッジ・「確認できていない」の断り・慣習の印は置かない）。ただし**特定名称8種の表の値だけは告示から写したまま**にし、出典と取得日を出典タブに残す。タブと小見出しの文言は `src/ui/Learn/outline.ts` の1箇所だけが持つ。
- **「知る」に説明用の作り値を置かない。** 6軸の図は**枠と軸線とラベルだけ**（架空の値の多角形を描かない）。「推定で埋めた値は無い」と書いている面に、実データに見える作り物を混ぜない。
- **クレジットの置き場: さけのわは全画面のフッタに1行**（5タブすべてがさけのわデータを使うため）、**地図の CC-BY 4項目は使用箇所（産地タブ）と「知る」タブだけ**（フッタには出さない。URL ルーティングが無く CC-BY §3(a)(2) の URI 枝に乗れないので使用箇所への併記で満たす）、**読みの CC-BY-SA 4項目は「知る」タブだけ**（地図と違い**ライセンス対象そのものを描く画面が無い** — 読みは銘柄を探す鍵で、出るのは当たった読み1つだけ）。**同梱データ自身にも `copyright` を持たせる**（ファイル単位で再配布されうるので、バンドルのクレジットとは別に検査する）。**文字列の有無は `attribution:check`、描画は単体テストが担う分担を崩さない**（増やすときは両方に足す）。
- **端末間の同期は「判断を1箇所、運搬を1箇所」に割る(B69 / PHASE 8)。** どちらを採るかは `src/domain/syncMerge.ts` の `planSync`(純関数)だけが決め、`src/store/sync.ts` は取ってくる/当てる/送るだけを持つ。サーバ(`server/`)は**運ぶだけ**で、保管の一貫性(「新しいほうだけ採る」)を SQL の1行に留める。両側に判断を置くと必ずずれ、壊れ方が「片方の端末の変更が黙って消える」なので気付けない。
- **2つの時刻を兼ねさせない。** `lastSyncedAt` は**端末の時計**(ローカルの `updatedAt` と同じ系。「前回より後に自分が触ったか」の判定)、位置は**サーバが振る整数のカーソル**。兼ねると、時計がずれた端末が過去の時刻で書いた行を、そこを通り過ぎた端末が二度と受け取らない。取り違えると `Date.parse(42)` が2042年として通り、push が例外も無く空になる。
- **やり取りする形は `src/domain/syncWire.ts` の1箇所**で、`server/src/index.ts` がこれを直接 import する。ただし**サーバは記録の中身を見ない**(外側だけ検証する2段の型ガード) — 中身まで見ると `SakeRecord` に項目を足すたびに、サーバを再デプロイするまで新しい記録が保存できなくなる。
- **リモート由来の反映に `deleteRecord` / `updateRecord` を再利用しない。** 前者は必ず削除の記録を書く(自分が消したのではないのに書くと押し返して往復する)、後者は存在しない id で例外を投げる(同期中に本人が消していると同期全体が止まる)。専用の `applyRemoteRecords` / `applyRemoteAliases` を通し、**同期を始めた時点の `updatedAt` と一致するときだけ**当てる(通信の間に本人が保存した編集を消さない)。
- **順序を崩さない。** 写真を取り切ってから1つのトランザクションで当てる(途中で通信を待つと IndexedDB が自動コミットして以降の書き込みが黙って落ちる)/ 写真を記録より先に送る(記録が見えてから写真が届くまでの隙間に別端末が同期すると、写真の無い記録が固定される)/ **送信が成功してから**位置を進め、送れた分だけ削除の記録を捨てる。
- **手動紐付けも同期する。** records だけだと片方の端末でだけ `寫楽` が未紐付けに戻る。`BrandAlias` は照合用の型なので更新時刻を足さず、保存形 `StoredAlias`(store 層)に持つ。時刻を持たない既存行は `DB_VERSION` 3 の版上げで `OLDEST_UPDATED_AT` を入れる — **入れないと初回同期で送られず、サーバ側に削除が在れば競合にも出ずに消える**(実測)。
- **同期の秘密は「利用者が決めたパスワード1つ」で、利用者名を持たない。** ランダムな値を貼り付ける形にすると新しい端末のたびに控えが要るので、覚えられる言葉を許す代わりに**サーバ側で回数制限**をかける（接続元ごとに15分10回。`server/src/index.ts` の `recentFailures`）。**正しく入力できたら打ち間違いの記録を消す** — 溜め込むと次の日の1回の打ち間違いで締め出される。ランダムな値なら回数制限は要らないが、**どちらか片方だけを外すと守りが消える**ので対で扱う。
- **取り込み(全置換)と全データ削除の後は同期の位置を捨てる。** 捨てないと、消えた記録がサーバに残ったまま降りてこず、取り込んだ記録も古い `updatedAt` のせいで1件も送られない(どちらも例外を出さず画面は正常に見える)。
- **鍵が決定的なストアは、書き直しのときに削除の記録を取り消す(B80)。** `planSync` は生きている行と削除の記録が**同じ鍵で同居したら、時刻を見ずに「消した」を採る**。それが正しいのは記録の `id` が uuid で、消して作り直せば別の鍵になるから。**メモ(`noteKey`)と紐付け(`aliasKey`)の鍵は決定的**なのでこの前提が崩れ、消す→書き直す→同期 で**相手の端末から消える**(書いた本人の画面には残るので気付けない)。`putNote` / `putAlias` は保存と削除の記録の取り消しを**1トランザクション**で行う(`deleteNote` / `deleteAlias` が逆向きを1トランザクションで保証しているのと対)。
- **書いたあとの同期は `syncAfterWrite`、同期のあとの読み直しは `reloadSynced`、それぞれ `src/App.tsx` の1箇所。** 経路ごとに書くと**足した日に1箇所だけ抜ける** — 実際、メモを足したときに保存も削除も同期を蹴っておらず、書いたメモがアプリを開き直すまで端末から出なかった(記録の削除と手動紐付けにも同じ穴が在った)。書き込みを増やすときは両方から呼ぶ。
- **`SyncResult.conflicts` には全種類を混ぜる。** 1つでも落とすと、負けたほうが画面に一言も出ないまま消える。
- **3つの版はそれぞれ別の理由で上がる。** `SYNC_SCHEMA_VERSION`(`syncWire.ts`。**サーバを先、アプリを後にデプロイする** — 上げないと旧サーバが知らないキーを捨てて 200 を返し、端末は位置を進めてその変更を二度と送らない) / `DB_VERSION`(`store/db.ts`。**移行が不要でもストアを増やしたら上げる** — 上げないと既に開いている端末に新しいストアが作られない) / `SCHEMA_VERSION`(`backupSchema.ts`。**新しい項目は任意にする** — 必須にすると手元の古いバックアップが「形が違う」で門前払いになる。**その項目を持たないファイルでは既存に触らない** — 0件で置き換えると古いバックアップを1回戻すだけで全部消える)。
- **銘柄・蔵元のメモ(B76)は `BrandNote = { target, targetId, text }` の1つの型で、鍵に種類を焼き込む。** 銘柄IDと蔵元IDは別の名前空間なのに値域が重なる(**銘柄ID 3264件のうち1352個が蔵元IDとしても存在する**。どちらも `number` なので型でも実行時でも止まらない)。**空文字の行を持たない** — 空にする操作は削除に落とす(許すと「消した」の表現が2通りになり、同期の勝ち負けで消したメモが空の行として復活する)。記録1件ごとの `SakeRecord.note` とは別物。
- - URL ルーティングを持たない（共有機能がスコープ外なので共有する URL が無い）。オーバーレイの開閉だけ `history.pushState(null,'',location.href)` + `popstate` で戻るボタンに対応する。

## リポジトリ作法

- 進捗・課題の正典は `docs/BACKLOG.md`。コミットメッセージに課題ID（`B1:` 等）を付ける。
- 生成物でもコミットするもの: `public/data/sakenowa/*.json`（さけのわ）、`public/*.png`（アイコン）。
- **コミットしないもの: `data/seed/`**（203本の日付・店名・備考。public リポジトリなので）。回帰テストは `src/domain/*.cases.json`（日付なし / 銘柄名なしに射影した2分割）が担う。
- `.env` 系は一切使わない（このアプリは環境変数を必要としない）。`.env.example` は hook と permission で読み書き両方ブロックされている。

## 環境の癖 / gotcha

- **`npm test` と `npm install` は素だと許可設定でブロックされる**（引数が必須）。`npm test -- <pattern>` / `npm install <pkg>` / `npm ci` を使う。`node scripts/*.mjs` も直接叩くと確認が出るので `npm run <name>` から呼ぶ。
- **Vitest 設定は `vitest.config.ts` に分離してある。** Vite 8 (rolldown) と vitest 同梱 vite の `Plugin` 型が衝突するため、プラグインは `vite.config.ts`、`test:{}` はこちら。統合しない。
- **`100vh` を使わない（`h-dvh` / `100dvh`）。** iOS は URL バー込みの高さになり下端のタブが画面外に出る。**Chromium では `dvh == vh` で再現しないのでブラウザ自動化では検出できない** — 実機スクショだけが証拠になる。
- `env(safe-area-inset-*)` は `index.html` の `viewport-fit=cover` が無いと 0 になる。
- **Service Worker の必須シェルは `cache.addAll` の原子性を保つ。** install 全体を best-effort にすると欠落したまま「成功」が記録されて再試行されず、オフライン起動が恒久的に壊れる。さけのわデータも必須シェル側（無いとサジェストが空になり新規記録ができない）。
- **Node 専用の型をアプリ全体の `types` に足さない。** 必要なテストファイルだけで `/// <reference types="node" />` する（`process`/`Buffer` が本番 `src` に漏れるとバグを隠す）。
- `<img>` に `width`/`height` 属性を付けたら CSS で `height:auto` を当てる（付けないと縦に伸びる）。
- さけのわ API は **CORS ヘッダを返さない**。実行時 fetch は不可能なのでビルド時取得＋コミット。
- **同期サーバのローカル実行は `.dev.vars`(gitignore 済み)。** `.env` 禁止はアプリ側の方針で、そちらは維持している(同期先の URL もトークンも環境変数にしない — URL は `src/config/app.ts` の定数、トークンは `meta` ストア)。
- **`server/` はアプリの `tsc -b` に入れない**(型検査は `npm --prefix server run check` に分ける)が、**eslint の対象からは外していない** — 認証と SQL の勝ち負けが乗っていて、無検査で置くほうが危ない。React のプラグインだけ当てない。
- **prettier は依存に入れていない（整形は eslint）が、`.prettierrc` は置いてある。** `.claude/hooks/format.sh` は `node_modules/.bin/prettier` が無いと PATH の prettier に落ちるので、**環境に prettier が入っていると既定設定（セミコロン・ダブルクォート）で書き換えられる**。設定ファイルはそれを既存の書き方（セミコロンなし・シングルクォート・100桁）に固定するためだけに在る。eslint はこの差を見ないので、無いと気付かないまま混ざる。

## コンテキスト維持

- 変更したファイル一覧・実行中のテストコマンド・未解決の課題(BACKLOG)を要約に必ず残す。

## 参照

- **進捗・課題の正典: `docs/BACKLOG.md`（着手前に必ず読む）** / 仕様: `docs/SPEC.md` / 実装計画: `docs/PLAN.md`（7フェーズの index）
- 開発プロセス: `docs/README.md`（`/spec`→`/plan`→`/phase-review`→`/release`）
- パス別ルール: `.claude/rules/` / 起動可能ワークフロー: `.claude/skills/`
