<!-- 例(stub)。常時ロード(paths無し)。強制が要るものは hook / deny ルールへ(これは指針)。 -->
# セキュリティ規約

- 秘密情報は `.env` のみ。コード・ログ・コミットに直書きしない(`.env` は gitignore 済み、編集は hook でブロック)。
- 外部入力は境界で検証する(SQL/XSS/コマンドインジェクション)。
- 不可逆・対外的な操作(本番デプロイ・公開・課金本番・破壊的コマンド)は実行前に確認を取る。
- 依存追加時はライセンスと既知脆弱性を確認する。
